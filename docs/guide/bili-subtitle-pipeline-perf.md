---
title: "B站字幕抓取链路 · 效率分析"
description: "B站字幕抓取链路效率实测（2026-09-18）· 所有数字均为本机实跑，非估算"
---

# B站字幕抓取链路 · 效率分析

> 2026-09-18 实测。所有数字都是这台机器上跑出来的，不是估算。
> 相关代码：`scripts/bili_analyze.py` · `bili_fetch_subs.py` · `bili_bench.py` · `bili_bench_retry.py` · `bili_audit_subtitle_content.py`

---

## 〇、结论速览

| # | 瓶颈 | 量级 | 现状 | 已改 / 待改 |
|:--:|:---|:---|:---|:---|
| **1** | **没有输出校验 ⇒ 68% 的数据是错的，白跑一整轮** | 浪费 ≈ **41 分钟**机器时间 | 首轮 1526 个文件里 1038 个内容是别的视频的 | ✅ 已加 `aid+cid` 不变量校验 |
| **2** | **重试尾巴**（拿不到正确轨的分P 跑满 40 次） | 占「难命中」视频 **88–98%** 的耗时 | 旧默认 40 次 × (110ms + 睡 550ms) ≈ **26 秒/分P** | ✅ 已降到 12 次 + 去睡 → **11x**；2026-09-19 **已落成默认值**（此前只存在于 CLI 参数） |
| 3 | 每请求新建 TLS 连接 | 单请求 **26%** | 110.3ms → 复用后 81.8ms | ⬜ 未做 |
| 4 | 并发度只有 6 | 尾巴上 **1.5x** | jobs 6 → 24：15.6s → 10.4s | ✅ 2026-09-19 默认已改 **24** |
| 5 | 无「负缓存」 | 每轮重复浪费 | 注定拿不到轨的分P 每轮都重试 12–40 次 | 🟡 2026-09-21 **部分解决**：`--ytdlp-skip` 借外部权威源跳过**确认无轨**的 90 个分P（省 1080 次/轮，见 §七）。通用的「hopeless 轮数」仍未做 |
| 6 | `--force` 无断点续传 | 中断即全部重来 | 跑到 250/338 被中断，进度作废 | ✅ 已加 `--refresh` |

**一句话**：这套链路的效率损失**主要不是「跑得慢」，而是「跑得快但跑错了」**。
修正确性花了 41 分钟；修速度（11x）只省了 6 分钟。顺序不能反。

---

## 一、最大的一笔浪费：正确性缺失

### 事故经过

| 阶段 | 动作 | 耗时 | 结果 |
|:---|:---|:---|:---|
| 首轮全量 | 下载 1441 个分P | 12.7 分 | 报「新增 1103 / 失败 271」，看起来正常 |
| 抽样检查 | 读 `BV1husGzwEtZ` P1（标题「1.1函数的定义和定义域」） | — | 内容是**日本世田谷灭门案** |
| 全量指纹 | 用前 60 字做指纹分组 | — | **1038/1526（68%）** 与别的视频内容重复 |
| 修复 + 重下 | 加 `aid+cid` 校验后全量重下 | ≈ 28 分 | 校验通过率 91% |

### 为什么整整一轮都没发现

因为流水线的「成功」判据是 **「文件非空 + 没抛异常」**：

```python
txt = ba.srt_to_text(url)      # ← 返回了内容，没报错
tmp.write_text(txt)            # ← 写进去了
tmp.replace(outfile)           # ← 文件非空
_HIT += 1                      # ← 计为成功
```

而 B站 的错误响应**完全合法**：`code: 0`、`lan: "ai-zh"`、`subtitle_url` 非空、
内容是结构完整的 JSON。**唯一能区分真伪的信息在 URL 的路径里**（见下）。

> **可推广的教训**：判据必须能区分「我查了，答案是没有」和「我没查成」，
> 也必须能区分「拿到了数据」和「拿到了**对**的数据」。
> 只校验「非空 / 无异常」的流水线，会把基础设施故障伪装成业务结论 ——
> 而且**看起来一切正常**，这是它最贵的地方。

### 根因（可判定）

`x/player/v2` 对同一个 cid 连续请求 5 次，`subtitles[0].subtitle_url` 的哈希段**每次都不同**：

```
第1次  .../prod/1131930242291812...  → 3316 字「贵阳地摊豆豉火锅」
第2次  .../prod/1154240080835093...  → 9720 字「各位同学大家好我是晨哥…」  ← 对
第3次  .../prod/1132649649316122...  →   22 字「♪恍恍惚惚已经隔世♪」
第4次  .../prod/1133563672051022...  →   97 字「♪音乐♪♪音乐♪」
第5次  .../prod/1133591689980932...  → 9288 字「哈喽大家好我是刘三少…」
```

而同一响应里 `data.aid = 115424008083509`、`data.cid = 33332858087`，
**正确轨的路径恰好是 `prod/{aid}{cid}{hash}`**：

```
正确:  prod/115424008083509 33332858087 78612aa9e02dd1f3945769fcfad52d25
                └── aid ──┘ └── cid ──┘
错误:  prod/1131930242291812...   ← 别的视频的 aid+cid
```

抽 8 个 cid 验证：**凡满足该不变量的轨，内容全部与视频主题一致**。命中率约 15–25%。

---

## 二、运行时瓶颈：重试尾巴

### 吞吐完全由「未校验率」决定（真实运行数据）

| 批次 | 分P 数 | 耗时 | 分P/分 | 未校验率 |
|:---|---:|---:|---:|---:|
| 高数批3 | 336 | 2.9 分 | **117** | 3.3% |
| 高数批1 | 329 | 4.3 分 | **76** | 1.2% |
| 计算机 | 496 | 6.6 分 | **75** | 1.0% |
| 高数批2 | 251 | 7.8 分 | **32** | 18.7% |
| 英语 | 95 | 6.4 分 | **15** | 53.7% |

相关性极强。原因是**成本模型是长尾的**：

```
一个正常分P：  1 次探轨（110ms）+ 1 次下载（110ms）      ≈ 0.25 秒
一个无望分P： 12–40 次探轨 × (110ms + 睡 550ms)         ≈ 26 秒
                                                    ─────────
                                                    约 100 倍
```

对英语那条视频算一笔账：51 个无望分P × 40 次 = **2040 次探轨**，
而全部 95 个分P 的正常需求只有 95 次 —— **尾巴占了 98% 的请求量**。

### A/B 实测：压尾巴的收益（30 个「已知难命中」分P，全部实测拿不到正确轨）

| 配置 | 耗时 | 校验通过 | 每分P | 加速 |
|:---|---:|---:|---:|---:|
| **A** 现状 `retries=40 sleep=0.3-0.8 jobs=6` | 145.0 s | 0/30 | 4.83 s | 1x |
| **D** 只去睡 `retries=40 sleep=0.05-0.15 jobs=6` | 50.9 s | 0/30 | 1.70 s | **2.8x** |
| **B** 调参 `retries=12 sleep=0.05-0.15 jobs=6` | 15.6 s | 0/30 | 0.52 s | **9.3x** |
| **C** 再提并发 `retries=12 jobs=24` | 10.4 s | 0/30 | 0.35 s | **14.0x** |

拆解：**去睡贡献 2.8x，降重试贡献 3.3x，两者相乘 ≈ 9.3x，再提并发 1.5x ≈ 14x。**

`sleep` 是可以安全去掉的 —— 实测 `jobs` 到 24 都**没有触发 412 限流**
（首轮 6 线程跑 1441 个分P，失败 0 例；`412` 一次都没出现）。这个 sleep 纯属保守。

### 端到端验证（英语，最坏情况）

| | 耗时 | 结果 |
|:---|---:|:---|
| A 默认参数 | 95 分P / **384 秒** | 未校验 51 |
| B 优化参数 | 52 个难分P / **33 秒**（等价难尾 362s → 33s） | 未校验 51 |

**11x，且准确率无损失**（两边都是 51 个未校验）。

> 取舍说明：降 `retries` 会让少数分P 本轮拿不到正确轨。这不是损失 ——
> 它们是**概率性漏掉**，下一轮 `--refresh` 会自动补。**「一轮尽量收全但很慢」
> 不如「一轮跑快 11 倍、多跑一两轮」**，尤其当漏掉的那批本来就是长尾。

---

## 三、次要瓶颈

### 3.1 每个请求都新建 TLS 连接

| 项目 | 中位 | p95 |
|:---|---:|---:|
| TLS 建连 `aisubtitle.hdslb.com` | 46.5 ms | 47.8 ms |
| TLS 建连 `api.bilibili.com` | 49.2 ms | 52.5 ms |
| **新建连接**请求 `x/player/v2` | **110.3 ms** | 127.3 ms |
| **复用连接**请求 `x/player/v2` | **81.8 ms** | 93.6 ms |

`urllib.request.urlopen` 每次都会重新握手。改用 keep-alive（`http.client` 复用或
`requests.Session`）可省 **28.5 ms/请求 = 25.8%**。每个分P 两个请求 → 约 57 ms。

**验证方式**：`scripts/bili_bench.py` ① 段（`bench_fresh_request` vs `bench_keepalive`）。

### 3.2 并发度偏低

串行 → 6 线程已经是 **10x**（11 → 113 分P/分）。但 6 还不够：
尾巴上 `jobs=6 → 24` 又拿了 1.5x，而且**没有撞到限流墙**。

注意并发不是线性的：在「容易命中」的视频上 `jobs=8` 反而比 `jobs=4` 慢
（148 vs 156 分P/分），说明**服务端有软性节流**。建议 **16–24** 之间取，别盲目拉高。

**验证方式**：`scripts/bili_bench.py` ② 段（并发扫描）。

### 3.3 没有负缓存

注定拿不到正确轨的分P（如 `BV1X4411J792` 的 45 个、`BV1rg411h7Z1` 的 51 个）
**每一轮都会重试 12–40 次**。应该把它们记进 `_verify.jsonl` 并加一个
`hopeless: N`（已试过 N 轮），下一轮直接跳过或只试 3 次。

**预期效果**：第二轮起，难命中视频的耗时降到接近 0。
**验证方式**：连续跑两次 `--refresh`，第二次的总请求数应接近 0。

### 3.4 `--force` 没有断点续传

首轮跑到 250/338 被中断，进度全废（`--force` 忽略已有文件）。
`--refresh` 修掉了这个：它读 `_verify.jsonl`，只补「缺失」或「未校验」的分P。

### 3.5 工具本身的可信度也是效率问题

第一版内容审计脚本的学科词表没校准，`高数` 的密度中位数算出来是 **0.0**
（因为当时大多数文件确实是错的），于是得出「84% 可疑」的假警报 ——
**又花时间去排查一个不存在的问题**。

修法：① 主判据换成确定性的 `_verify.jsonl`；② 英语词表必须含英文词
（英语课字幕是中英混排，纯中文词表把正确文件打成 0.3 分/千字）。
修好后：英语中位数 0.3 → **5.4**，误报 146 个被显式标成「密度低但校验通过」。

> **教训**：**坏的量具比没有量具更贵** —— 它会让你去修没坏的东西。

### 3.6 我自己的 `--refresh` 也踩了同一类坑（2026-09-18 当场修掉）

第一版 `--refresh` 在每轮开始**清空 `_verify.jsonl`**，想避免旧的 `ok: true` 让
「其实已失效」的分P 被跳过。结果适得其反：清空之后，本轮没被处理到的分P
**变成「无记录」**，下一轮就当成「需要重下」。

实测代价：第二轮白下了 **1468 个分P**（本来只需要 133 个）。

修法：不清空，继续追加（读取时「后写的覆盖先写的」），跑完用 `_compact_verify()`
压成一个分P 一行。验证：再跑一次 `--refresh`，应显示「已有跳过 1335」而不是
「新增 1468」。

> 这和 §一 是**同一类错误的两个实例**：**把「我没有这个信息」当成了「这个信息是否定的」**。
> 一次在探测层（限流 → 无字幕），一次在缓存层（无记录 → 需重下）。

### 3.7 死路记录：WBI 签名端点也拿不到

怀疑 `x/player/v2` 不带 WBI 签名会走一条被污染的缓存路径（B站 对
`x/web-interface/view` 就强制要求 `wbi/view`，否则 412）。于是实现了 WBI 签名
（`img_key+sub_key` → 固定置换表 → 前 32 位 = `mixin_key` → `w_rid = md5(query+mixin)`），
对比 `x/player/wbi/v2` 与旧端点：

| BV | P | wbi 命中 | 旧端点命中 |
|:---|--:|--:|--:|
| `BV1rg411h7Z1` | 1 / 3 / 5 | 0/8 | 0/8 |
| `BV1X4411J792` | 52 / 60 / 90 | 0/8 | 0/8 |
| `BV1PRM26hEbk` | 1 | 0/8 | 0/8 |
| `BV1xzBCBFExz` | 1 | 0/8 | 0/8 |
| `BV1husGzwEtZ` | 13 | 0/8 | 0/8 |

**结论：两个端点一样，都没有正确轨。** 再枚举 20 次请求返回的全部去重 URL 逐个下载验内容：
这些分P 能拿到的只有「别的视频的 AI 轨」和一条 `lan=zh-Hans` 的
「敲重点↓↓↓…威信扫」广告轨 —— 而**这条广告轨在两条不同 UP 主的视频里返回的是同一份**，
说明它也不是该视频自己的字幕。

⇒ **~9% 是 `x/player/v2` 这条路的硬上限，不是代码缺陷。** 想再往上只能换通道（ASR）。
写脚本见 `scripts/bili_test_wbi.py`。

---

## 四、优化清单

| 优先级 | 改什么 | 文件 | 预期效果 | 验证方式 |
|:---|:---|:---|:---|:---|
| **P0** ✅ | 下载前校验 URL 含 `aid+cid`，结果落 `_verify.jsonl` | `bili_analyze.py::probe_subtitle` | 静默脏数据 **68% → 0** | `bili_audit_subtitle_content.py`：未校验率 < 10% |
| **P0** ✅ | `retries` 40→12，`sleep` (0.3,0.8)→(0.05,0.15) | `bili_analyze.py` 顶部 `PROBE_RETRIES` / `PROBE_SLEEP` | 尾巴 **9.3x**；端到端 **11x** | `bili_bench_retry.py`；英语 `--refresh` 384s → 33s |
| **P0** ✅ | ↑ **真正落成默认值**（2026-09-19 02:10）—— 在此之前优化**只存在于 `bili_fetch_subs.py` 的 CLI 参数里**，不带参跑 = 等于没优化，实测慢 ~10x | `bili_analyze.py:39-40` | 零传参即得 **~10x** | 探针 `PROBE_RETRIES==12`；**默认参数**跑 5 个难命中分P = **2.50 s/分P**（旧默认等效 ~26 s） |
| **P0** ✅ | `--refresh` 断点续传（替代 `--force`） | `bili_fetch_subs.py` | 中断不再作废进度 | 中断后重跑，跳过数 = 已完成数 |
| **P0** ✅ | 校验记录**追加**而非清空（+ `_compact_verify()`） | `bili_fetch_subs.py` | 避免第二轮白下 1468 个分P | 重跑 `--refresh` 显示「跳过 1335」而非「新增 1468」 |
| **P0** ✅ | 判定 9% 未校验为 **API 上限**而非继续加 `retries` | `bili_test_wbi.py` | 省掉无意义的追打 | wbi 端点 0/8、旧端点 0/8 |
| **P1** ⬜ | HTTP keep-alive 复用连接 | `bili_analyze.py::api` | 单请求 **-26%** | `bili_bench.py` ① 段 |
| **P1** ✅ | 默认并发 6 → **24**（2026-09-19 落地；原计划 16，实测 24 未触发 412） | `bili_fetch_subs.py::--jobs` 默认值 | 尾巴 **1.5x** | `bili_bench.py` ② 段 |
| **P1** ⬜ | 两阶段跑：pass1 低重试（快，收 90%）→ pass2 只对残留用高重试 | `bili_fetch_subs.py` | 兼得速度与收全率 | pass1+pass2 总时长 < 单轮高重试 |
| **P2** ⬜ | 负缓存：`_verify.jsonl` 记 `hopeless` 轮数 | `bili_fetch_subs.py` | 第二轮起难命中视频 ≈ 0 秒 | 连跑两次，第二次请求数 ≈ 0 |
| **P2** ⬜ | 审计器阈值改为「相对中位数 + 校验记录」双判据 | `bili_audit_subtitle_content.py` | 误报 146 个显式区分 | 报告里「密度低但校验通过」一栏 |
| **P3** ⬜ | 用 `asyncio` + `aiohttp` 替代线程池 | `bili_fetch_subs.py` | 理论上更高并发、更低内存 | 与 jobs=24 对比分P/分 |

---

## 五、当前状态与验收标准

### 当前状态（2026-09-18 实测）

| 指标 | 数值 |
|:---|---:|
| 分P 总数 | 1526 |
| 有文件 | 1526（100%） |
| 通过 `aid+cid` 校验 | 1382（**90.6%**） |
| 未通过校验 | **144**（合计 **53.3 小时**） |
| └ 集中在 | `BV1X4411J792` 52 个 / 14.5h · `BV1rg411h7Z1` 51 个 / 9.3h · `BV1husGzwEtZ` 11 个 / 8.7h · `BV1PRM26hEbk` 6 个 / 8.4h · 其余 24 个 |
| 需要 ASR 的分P | **90**（2026-09-21 用 yt-dlp 普查把 144 拆开后的结果 —— 原记 144 是**把「未校验」当成了「无轨」**，见 §七） |

> ⚠️ 早先「5 条视频无字幕、必须走 ASR」的结论**是错的**，已在
> `docs/guide/bili-plan/computer.md` 与 `bili-scraping.md` 中更正 ——
> 那是同一个静默失败 bug 的另一个表现。**真实缺口是 144 个分P，不是 338。**

### 验收标准

**⚠️ 先说清楚：「校验通过率 ≥ 97%」这个目标达不到，而且不该当目标。**
§3.7 已证明剩下 144 个分P 在 `x/player/v2` 与 `x/player/wbi/v2` 上都是 0/8 命中，
这是 API 层的硬上限。硬追这个指标只会变成把重试次数往上加 —— **纯浪费**。

| 项 | 标准 | 当前 | 判定 |
|:---|:---|:---|:---|
| 校验通过率 | **≥ 90%**（API 上限约 91%） | 90.6% | ✅ |
| 剩余未校验分P | 已明确列清单 + 标注时长，作为 ASR 待办 | 144 个 / 53.3h | ✅ |
| 脏数据不得进入提炼 | 144 个分P 的内容**不得**出现在知识点成品里 | 待执行 | ⬜ |
| 续传 | 重跑 `--refresh` 跳过数 = 已完成数 | 跳过 1335 | ✅ |
| 单轮全量耗时 | < 10 分钟 | 优化参数下约 3–5 分 | ✅ |
| Markdown 安全 | `check-md-hazards.mjs` 无新增告警 | ✅ 扫描 316 / 跳过 17 | ✅ |
| 链接完整 | `check-links.mjs` 无死链 | ✅ 333 文件 / 3173 链接 | ✅ |
| 渲染验证 | `verify-notes-render.mjs` 全通过 | 待新文档渲染后跑 | ⬜ |

---

## 六、下一步

### 6.1 字幕链路：已到 API 上限，收尾动作只有两件

```bash
# ① 再跑一轮确认没有新进展（预期：跳过 1382、新增 ≤ 144）
python scripts/bili_fetch_subs.py --refresh --jobs 24 --probe-retries 12 --probe-sleep 0.05,0.15

# ② 生成最终清单（含未校验分P 的时长，作为 ASR 待办）
python scripts/bili_audit_subtitle_content.py
```

**涉及文件**：`scripts/bili_fetch_subs.py`、`scripts/bili_audit_subtitle_content.py`、
`data/bili-analyze/<BV>/_verify.jsonl`、`data/bili-analyze/_subtitle_audit.md`

**期望输出**：`_subtitle_audit.md` 明确列出 144 个未校验分P / 53.3 小时。

**判定为「已收尾」的标准**：再跑 `--refresh` 时**新增数为 0 或仅个位数**
（说明剩下的不是「还没试够」而是「真的拿不到」）。当前已满足。

**要不要对这 144 个分P 跑 ASR？** 建议**先不跑**：
- 144 个分P / 53.3 小时 ≈ 1.6 GB 音频，转写是几十小时量级的任务
- 其中 `BV1X4411J792`（52 个）与 `BV1rg411h7Z1`（51 个）是**冗余课**，
  同一考点在 `BV1Up4y1Y76a` / `BV1swAWerEzS` / `BV1brgBzNEbW` 上都有且字幕完整
- 真正需要 ASR 的是**无替代源**的部分：`BV1PRM26hEbk`（政治 mayi 基础班 8.4h）
  与 `BV1TDj36dEdc`（政治 5.7h）—— 政治只有这 19 个分P，没得选

⇒ 建议只对 **政治 11 个分P / 14.1 小时** 跑 ASR，其余 133 个先挂账。

### 6.2 知识点提炼（**已完成** —— 本节原写「未开始」，2026-09-21 核实后更正）

> **实际状态**：`docs/guide/knowledge-map/` 已产出 **9 个文件 / 约 590 KB / 1138 条知识点 / 18 门课**
> （`computer/{basics,c-language,c-advanced,data-structures}.md`、`math/{limits-derivatives,integrals,advanced}.md`、
> `english.md`、`index.md`）。**无政治** —— 与「政治 0/19 全无轨」一致。
> 四步压缩：1526 分P → 79 个单元 → 2451 条卡片 → 合并 2116 条 → 去重 **1138 条**。
>
> **剔除口径已核对一致**（2026-09-21）：`_units_verified/_manifest.json` 记录 58 个部分剔除，
> 另有 **6 门课整单元丢弃**（政治 5 + 英语 1 = 70 个分P），合计 128；
> 与期望的 125 差 3，差的 3 个是 `BV1X4411J792` p70/86/95 —— 它们在 `_verify.jsonl` 里
> **完全没有记录**（从未被校验过），被过滤器的「无记录 = 未校验 = 丢弃」安全默认挡掉。
> ⇒ **无脏数据泄漏，只是 3 个「没查过」的分P 被保守排除**，属可回收项。
>
> ⚠️ **核对方法上的坑**：不能只读 `_manifest.json` —— **整门课被丢弃的单元根本不在 manifest 里**，
> 只看 manifest 会得出「漏剔 70 个」的假结论。必须把「部分剔除」与「整单元丢弃」两份记录合起来算。

以下验收标准保留，作为**复核清单**：

**语料**：`data/bili-analyze/_units/*.txt` —— 已切好的 **69 个工作单元 / 484 万字符**
（计算机 20 · 高数 37 · 政治 5 · 英语 7）。

**方法**：分片提炼 → 合并。让每个子代理读一个单元、把结构化结果写到
`data/bili-analyze/_extract/<unit>.md`，我再合并去重。

**⚠️ 前置约束（硬性）**：**144 个未校验分P 的内容不可信，必须在提炼时排除**。
否则会把「火锅探店」「篮球解说」当成知识点提炼进笔记。
具体做法：提炼前先按 `_verify.jsonl` 把单元里的 `=== P12 | ... ===` 段落
按 P 号过滤掉未校验的；若某个单元被过滤后剩余不足 30%，整单元跳过并在报告里列出。

**输出格式**（每个知识点）：
- **要点**：一句话说清结论，具体到能直接做题
- **关键概念**：核心术语
- **关系**：与相邻知识点的因果 / 前提 / 对比 / 推广
- **出处**：P 号（可回溯）

**最终交付**：`docs/guide/knowledge-map/` 下分科文档 + 总览索引。

**验收标准**：
| 项 | 标准 |
|:---|:---|
| 单元覆盖 | 69/69 都有 `_extract/*.md`（被过滤跳过的要在报告里列出并说明原因） |
| 无凭空生成 | 每个知识点都能回指到某个 P 号 |
| **不掺脏数据** | 144 个未校验分P 的内容**不出现**在成品里（可抽查：成品里不应出现「火锅」「球赛」「音乐♪」等） |
| 主题命名 | 用学科通用章节名（「极限」「导数与微分」），不自造词 |
| 构建安全 | `check-md-hazards.mjs` 通过 |
| 渲染验证 | `verify-notes-render.mjs` 全通过 |

### 6.3 建议但未做的性能改动

按 §四 的顺序：**keep-alive（-26%/请求）→ 默认并发 6→16（1.5x）→ 负缓存**。
这三项都不影响正确性，属于「顺手就做」。真正**不该**做的是继续加 `retries`
去追那 9% —— 那是把时间丢进一个 API 上限里。

---

## 附：复现命令

```bash
# 延迟与并发基准
python scripts/bili_bench.py

# 重试参数 A/B（用 30 个已知难命中分P）
python scripts/bili_bench_retry.py

# 内容审计（主判据 = _verify.jsonl）
python scripts/bili_audit_subtitle_content.py

# 优化参数全量重下
python scripts/bili_fetch_subs.py --refresh --jobs 24 \
    --probe-retries 12 --probe-sleep 0.05,0.15
```

---

## 七、2026-09-21 增补：yt-dlp 普查 —— 第三条判据，把「未校验」拆成三类

### 7.1 为什么需要它

`_verify.jsonl` 里的 `ok:false` **只有一种语义**（「本轮没通过 aid+cid 不变量」），
但它把两件性质完全不同的事混在了一起：

| 实际情形 | 该怎么处理 |
|:---|:---|
| 这条视频**压根没有字幕轨** | 只能走 ASR，重跑一万次也没用 |
| **有轨**，只是本轮没拿到正确的那条 | 重跑有救，不该进 ASR 清单 |

混在一起 ⇒ **ASR 待办清单虚高**（§五 原来那个「144」就是这么来的）。

而且 `_verify.jsonl` 记的是「**上一轮的结果**」，不是「这条视频到底有没有轨」——
对同一个分P，它的回答会随轮次变化。所以需要一条**与下载历史无关**的独立判据。

### 7.2 用 yt-dlp 做一次独立普查

`scripts/bili_ytdlp.py` 只做一件事：**声明探测**。它问 B站 的 `x/player/wbi/v2`
（与自建脚本同一个端点），**只看 `subtitle.subtitles` 有没有条目** —— 不下载、不判内容。

> **它不能替代自建脚本**：yt-dlp 的 info dict **不暴露 aid/cid**，也就**没有** aid+cid
> 这个安全网。所以分工是：**归属判定归自建脚本，轨有无的普查归 yt-dlp。**

**四个必须知道的坑**（逐层实测才走到正确答案，前三个都是「看起来对」的错答案）：

1. **「返回空 dict」不等于「没有」** —— 未登录时 B站 不给 AI 字幕（`data.need_login_subtitle`）。
2. **yt-dlp 默认根本不查字幕** —— 必须显式 `listsubtitles=True`。不加时 `subtitles` **恒为 `{}`**，
   而日志照样打印 `Extracting subtitle info <cid>`。**沉默的默认值伪装成了否定结论。**
3. **`subtitles` 里的 `danmaku` 不是字幕** —— 是 yt-dlp 在 `_get_subtitles()` 里**硬编码**塞进去的
   弹幕 XML（`https://comment.bilibili.com/{cid}.xml`），**每个视频都有**。
   不剔除它，会把**每一个**视频都判成「声明有轨」—— 反向假阳性。
4. 剔除 `danmaku` 后，结论才与本地 `_verify.jsonl` 吻合。

逐 BV 明细落在 `<BV>/_ytdlp.jsonl`，`source` 字段三态：
`yt-dlp:claimed` / `yt-dlp:no_track_authenticated` / `yt-dlp:unknown_unauthenticated`
（**只有第二态可用于跳过** —— 第一、三态都可能「其实有轨」）。

汇总报告：`scripts/bili_ytdlp_report.py` → `data/bili-analyze/_ytdlp_scan.md`。

### 7.3 普查结果：144 个「未校验」拆成三类

| 轨（yt-dlp 普查） | recovered | junk | unjudged | 合计 |
|:---|---:|---:|---:|---:|
| **无轨**（确认拿不到） | 0 | 20 | 70 | **90** |
| 声明有轨 | **19** | 34 | 0 | **53** |
| 未普查 | 0 | 1 | 0 | 1 |
| **合计** | **19** | **55** | **70** | **144** |

分科明细：

| 科目 | 总 P | 未校验 | 无轨 | 声明有轨 | 未普查 | recovered | 每轮省请求 | BV 数 |
|:---|---:|---:|---:|---:|---:|---:|---:|---:|
| 计算机 | 496 | 5 | 3 | 1 | 1 | 0 | 36 | 8 |
| 高数 | 913 | 69 | 17 | 52 | 0 | 19 | 204 | 7 |
| 政治 | 19 | 19 | **19** | 0 | 0 | 0 | 228 | 5 |
| 英语 | 95 | 51 | **51** | 0 | 0 | 0 | 612 | 4 |
| **合计** | **1523** | **144** | **90** | **53** | **1** | **19** | **1080** | **24** |

**★ 与独立来源交叉验证（这是本判据可信的关键）**：

- **政治**：普查口径「19 个全部确认无轨」，与 `_subtitle_audit.md` 的通过率 **0/19** **精确吻合**。
  两条路径彼此独立（一条问 yt-dlp、一条统计下载结果），却给出同一个数。
- **英语**：51 个全在 `BV1rg411h7Z1`，与 §3.3 记的「51 个」吻合。
- **`unjudged` 70 个全部落在「无轨」组** ⇒ 两条独立路径指向同一批分P，不是巧合。

### 7.4 由此更正 §五 的一个数字：真无轨是 90，不是 144

§五 写「需要 ASR 的分P = 144」。实测**真无轨 = 90 个**（这 90 个才需要 ASR）；
另外 53 个是「声明有轨但本轮没拿到正确轨」，**重跑有救**，1 个未普查。

再往前一步：`scripts/bili_reclassify_unverified.py` 用**内容判别**给这 53 个做二次判定 ——
以同 BV 内已校验通过的分P 作参考语料，算字符二元组余弦相似度 + 标题重合度：

> **19 个判为 `recovered`** —— 内容其实是对的，当初只是 URL 没匹配上不变量。
> 全部落在 `BV1X4411J792`（p58/60/62/66/68/69/71/72/80/82/83/88/90/94/97/98/106/110/112）。

⚠️ **注意这 19 个在 `_verify.jsonl` 里仍然是 `ok:false`** —— 判 recovered 是另一份文件
（`_verify2.jsonl`）的结论，两者不会自动同步。

⇒ **真正的 ASR 待办 = 90 个**。
（**不是 71** —— 那 19 个 recovered 本来就在「声明有轨」组里，**不该从 90 里减**。
把不同组的数相加相减是这类统计最容易犯的错。）

### 7.5 `bili_fetch_subs.py` 新增的三个开关

| 开关 | 作用 | 为什么默认这么设 |
|:---|:---|:---|
| `--ytdlp-skip` | 跳过 `_ytdlp.jsonl` 判为**确认无轨**的分P，不做探测 | **只认 `yt-dlp:no_track_authenticated` 这一态**；未登录的空结果、`claimed` 都不跳过 |
| `--recheck-recovered` | 允许 `--refresh` 重下 `_verify2.jsonl` 判为 `recovered` 的分P | **默认保护**：那些文件内容是对的，而重下很可能正是拿回当初那条脏轨 —— 等于用好数据换坏数据 |
| `--dry-run` | 只打印本次会下载哪些分P，不发任何请求 | 长任务起跑前先看范围，尤其确认 `--force` 的覆盖面 |

**`--ytdlp-skip` 的实际收益**：24 条 BV 共 90 个分P 确认无轨 ⇒ **每轮省 90 × 12 = 1080 次**
探测请求。逐 BV 最明显的是 `BV1rg411h7Z1`（51 P 全部无轨）：任务数 **51 → 0**。

**`--recheck-recovered` 是护栏，不是功能**：它存在的唯一理由是防止 `--refresh` 把 19 个
`recovered` 分P 覆盖掉（它们在 `_verify.jsonl` 里仍是 `ok:false`，所以 `--refresh` 会重下）。

**`--dry-run` 的语义**：预览**不算破坏性动作**，所以它排在 D5 护栏**之前** ——
`--force --dry-run` 会打出「将覆盖 N 个文件，其中 M 个已校验」并提示「真跑会被拒绝」，
但退出码仍是 0。实测 2245 个文件零改动。

### 7.6 复现

```bash
# ① 普查（写 <BV>/_ytdlp.jsonl）
python scripts/bili_ytdlp.py BV1X4411J792        # 单条
python scripts/bili_ytdlp.py --batch bvs.txt     # 批量：每行一个 BV 号

# ② 汇总成全局表
python scripts/bili_ytdlp_report.py              # → data/bili-analyze/_ytdlp_scan.md

# ③ 预览这次会下什么（不发请求）
python scripts/bili_fetch_subs.py --refresh --ytdlp-skip --dry-run

# ④ 真跑（省掉 1080 次无用探测）
python scripts/bili_fetch_subs.py --refresh --ytdlp-skip --jobs 24
```

**涉及文件**：`scripts/bili_ytdlp.py`、`scripts/bili_ytdlp_report.py`、
`scripts/bili_fetch_subs.py`（`--ytdlp-skip` / `--recheck-recovered` / `--dry-run`）、
`scripts/bili_reclassify_unverified.py`、`data/bili-analyze/<BV>/_ytdlp.jsonl`、`_verify2.jsonl`。
