# 统一换行符：所有文本文件入库规范化为 LF，杜绝 CRLF/LF 幻影修改
#
# ★ 2026-09-20 补 `eol=lf`（修 E2 的系统性那一半）。
#   原来只有 `text=auto`，而 `text=auto` **只管入库方向**：提交时把 CRLF 规范成 LF，
#   但检出方向仍受 `core.autocrlf=true` 支配 —— 于是工作区又被写回 CRLF。
#   实测：`git ls-files --eol` 显示 991 个文件是 `i/lf w/crlf`，
#   即上面那句注释承诺的效果**并没有发生**。
#   `eol=lf` 把检出方向也钉死为 LF，注释才名副其实。
#
#   同时修掉一个边界：`docs/posts/computer/2025.md`、`2026.md` 在索引里被标成
#   `-text`（git 判成非文本），这类文件**完全不做行尾规范化** ——
#   `eol=lf` 让它们也回到受管状态。
#
#   本仓库没有 .bat / .cmd（那两个类型需要 CRLF），已确认过再改。
* text=auto eol=lf

# 脚本必须强制 LF：在 Windows 上被检出成 CRLF 时，
# `set -euo pipefail` 会变成 `set -euo pipefail\r`，bash 报错或行为异常；
# 而 GitHub Actions 的 Linux runner 又要求 LF。
# 显式锁定 eol=lf，避免依赖各平台 core.autocrlf 的默认值。（2026-09-17 补）
*.sh      text eol=lf
*.bash    text eol=lf
*.py      text eol=lf
*.mjs     text eol=lf
*.json    text eol=lf
*.yml     text eol=lf
*.yaml    text eol=lf

# 明确的二进制类型（防止误规范化）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.pdf binary
*.woff binary
*.woff2 binary
*.ttf binary
*.zip binary
*.gz binary
*.db binary

# 2026-09-17 补充：其余二进制类型（此前未声明，存在被误当文本规范化的风险）
*.bmp binary
*.tif binary
*.tiff binary
*.otf binary
*.eot binary
*.doc binary
*.docx binary
*.xls binary
*.xlsx binary
*.ppt binary
*.pptx binary
*.epub binary
*.rar binary
*.7z binary
*.tar binary
*.mp4 binary
*.mov binary
*.mkv binary
*.avi binary
*.mp3 binary
*.wav binary

# 2026-09-17 补充：GitHub 语言统计排除生成物与第三方副本
# （否则 dist / node_modules / ForestDeer 副本会把 HTML/JS 占比刷上去，
#   掩盖本仓库真正的主体——Markdown 笔记）
docs/.vitepress/dist/**  linguist-generated
docs/.vitepress/cache/** linguist-generated
node_modules/**          linguist-vendored
guangdong-zhuanshengben-resources/** linguist-vendored
