# 🟩 C 语言 · 话题开场卡

> 新话题第一句只说「**继续 C 语言**」。AI 必须按本卡接手，**禁止从头问起、禁止重新自我介绍**。

## AI 开工三步（硬性）

1. **先读** `备考计划/学习进度看板.md`（唯一事实源）→ 看「🔍 待验证区」+「📈 科目进度条 · C 语言」
2. **再查课本原文**（禁止凭记忆讲 C 语法）：
   ```bash
   cd /d/deeepseek/zhuan-sheng-ben-notes && node scripts/textbook.mjs <概念词>
   ```
   引用一律标「第4版第N页」；**无命中禁止编页码**，改标 ⚠️ 推算
3. **按 `skill:step-teacher` 教**：一次 1 个概念 → 符号翻译成人话 → 讲完立刻练 ≤3 题

## 教学纪律（两次事故换来的，不可退让）

| 规矩 | 由来 |
|:---|:---|
| 一次只教 **1 个**新概念 | 09-13 一次灌 9 个考点 → 紧接着 3 题全错且同根 |
| **没教过的不出题** | 09-10 只教了三行框架就考 for 循环，他答 4 |
| 判卷**先看过程、后看数字** | 只给数字不给过程 = 一律按错题重做 |
| **撤掉脚手架**答对才算掌握 | 09-10 有表格填对、撤表格答 6 = 假掌握 |
| 相邻两题**答案刻意错开** | 抓"搬上题答案"的探针（#C002 就是这么暴露的） |
| 教练自己错了**当场改账并明说"不记学生错"** | 09-13 题面"共 5 行"是教练错，gcc 证伪 |
| 他一行一轮的写法（`0+1→1+2→3+3`）**就算表格** | 他说过电脑上不好画表格，别再被格式卡住 |
| **每讲完一节，必问一句「这一节有没有哪句话你看不懂？」** | 09-14 晚间：8 题数字 5/5 全对（81%），他仍说「这一部分还是看不下去」→ **正确率抓不住"听不懂"**，判据有洞，须加这道人工检查。**只有他亲口说不懂才算真实反馈** |
| **每讲一个概念，当场报它在考卷的分量**（考不考、以什么形式考） | 09-14 他第 4 个自主提问是「这些全要背吗？这么多含义」→ 他要的是**权重**，不是再多一道题。不给 = 给他留焦虑 |
| **一次只留一个画面**，比喻和术语不混排 | 09-14 第五节首版一口气塞 6 层（两张票 + `%c` 不挑 + 清洁工空格 + `getchar` 扫把 + 三样输入 + `'\n'` 写法）→ 失败。推倒后**只留「点餐台排队票」一个画面**，他随即答对 |
| 演示代码**一行动 `a` 一次** | 09-14 教练写 `printf("%d", a++, a)` = **未定义行为**，输出不可作凭据（同一行既改又读同一变量，编译器爱输出啥输出啥） |

## 取证工具箱（C 语言最大优势：能真跑）

```bash
export PATH="/d/w64devkit/w64devkit/bin:$PATH"   # 不带这行 gcc 报 cannot execute 'as'
mkdir -p /tmp/c-practice && cd /tmp/c-practice
gcc t1.c -o t1.exe && ./t1.exe
```
临时 `.c` 只写 `/tmp/`，**不许落进仓库**。已实测结论可直接引用：
`a++` 后立刻偷看 `a` **已是新值**（"先汇报"≠"还没加"）· `5/3=1` · `7%2=1` · `strlen("...")=9 sizeof=10` · `%d` 接 float 印出乱数并警告。

## 当前欠账（09-14 晚间视角，按优先级）

> ✅ **09-15 更新：下面 🔴 ①②③ 三笔欠账已全部销账，下次别重复考，直接开 1.4 第二课。**
> ① 吃换行符换同型 `getchar` 版 → 答对 `a=2.5 / p=回车 / q=X`（并自己说出"一次只抱一张票"）
> ② `%5d` 逐字符 → 最终 `<···9>` = **6 个字符**全对（中途漏过空格与 `[`，订正后清楚"普通字符照抄、空格也算人头"）
> ③ `&` 方向性 → 他说出「**printf 是向外取出来，不用 `&`**」= 真理由
> 🆕 **1.4 第一课「条件=门卫」已过**：条件只能答是/否；**真=`1`、假=`0`**；**门卫只认 0，只有 0 是假，其他任何数（-8/100）都为真** —— 他 3 题全对且**自己推出**这条规则（生成的知识）。`==`（问）/ `=`（塞）语义已说清。
> 🆕 **09-15 用户拍板新节奏（覆盖旧默认）**：「**一天可以交很多新概念，但一次交一个概念**」→ 一个概念讲完**立刻只出 1 道题**，过了才交下一个，**禁止一次铺 3 题**。
> ⚠️ **教练侧一笔自记（不记学生错）**：我出了 `printf("%d", a=3)` 印几，那要用到**未教的**「赋值表达式本身也有值」→ 违反"没教过的不出题"，**该问已撤回**，留给下次正式教。
> 🔴 **下次 1.4 第二课顺序**：`if ... else`（门卫有两条路）→ 再练 `==` vs `=` 改错专项。
> 🔴 **09-15 页码系统偏差已查明**：`textbook.mjs` 输出的是 **PDF 物理页**，纸质书页码 = **物理页 − 17**（逐页读页脚实测恒定）。看板/讲义里旧写的 p61/p94/p98 一律按此换算；下次引课本**必须标书页码并注明换算**。

- 🔴 **① scanf 吃换行符**（下次首检第一题）：**必须换同型题** —— 原题 `scanf("%d",&a);scanf("%c",&ch);` 输入 `5⏎` 的答案**已写进讲义 1.3b 第五节 + 自检 Q7**，再用等于送分。
  **✅ 已 gcc 验过的备题（推荐直接用）**：`float a; char ch; scanf("%f", &a); ch = getchar();` 喂 `2.5⏎X⏎` → 实测 **`a = 2.5`、`ch` 编号 = 10（换行）**。一次换掉三处（`%d`→`%f`、`scanf("%c")`→`getchar()`、`5`→`2.5`），搬不了上题答案，**顺带复检"`getchar()` 不要 `&`"**。他 09-14 晚答对过原题 `a=5 / ch=回车` ✅ 但**只隔一天**
- 🔴 **② `%5d` 逐字符默写**：`printf("<%5d>",7)` → **7 个字符** = `<` + **4 个空格** + `7` + `>`。⚠️ **教练侧订正**：看板旧文写"`<···7>`（3 空格）"是我数错了，gcc 实跑 `<    7>` 是 **4 空格**，不记学生错。他从未默写过这题
- 🔴 **③ `&` 的方向性**（口述）：必须答出"**scanf 要往盒子里写 → 得给门牌号；printf 往外读 → 不用**"。**禁答"因为这是 scanf 语句"**（那是复述题目 = 循环论证，09-14 他首考就是这样）。✅ 已澄清的旁支：`ch = getchar();` **不要** `&`（有 `=` 就不用手写门牌号），gcc 实测 `getchar(&ch);` 报 `too many arguments`
- 🔴 **④ 逗号表达式取末值（带变量版）** → ⚠️ 原题 `(a++,a++,a++)` 的答案 09-14 被教练说破泄露，**判据作废，必须换同型**。备题 `int a=1; int x=(a++, a*3);` → **gcc 实测 `x=6 / a=2`**（逗号取最右 = `a*3`；但 `a++` 在前面已真把 a 抬到 2 —— 这题同时复检 #C003 的 `a++` 落地时机）。⚠️ 教练侧订正：本行旧版备题写作 `x=(a+5, a*3)` 并标"答 `x=6, a=6`"是**凭印象编的，错了**。gcc 独立实测（`int a=1; int x=(a+5, a*3);`）→ **`x=3 / a=1`**：`a+5` 算完**结果被扔掉**、也没赋给任何人，所以 **a 全程没变**，最后 `a*3` = 1×3 = 3。这正好是「逗号取最右，但前面那句若不改 a 就是白算」的反例题，要用就以 gcc 结果为准。
- ✅ ~~`a++` 落地时机~~ —— 09-14 销账（#C003）。⚠️ **约 09-17 复检**（n=2 封顶）
- ✅ ~~`%d` 坑位数 / 逗号两副面孔~~ —— 09-14 同日清账（首考 1/3 → 重做 **3/3**，#C004）。⚠️ 约 09-16 用带变量混合题复检：`printf("[%d和%d]", (a,7), a+1)`（gcc 实测 `[7和3]`）
- ✅ ~~整数除法截断~~ —— **09-14 晚已建立模型**：`printf("%d",7/2)`→3 ✅、`int i = 1/2;`→**0** ✅（**这行根本没有 printf**，正好自证刀在除法不在打印）。⚠️ 他嘴上仍用旧说法，约 09-16 复查"刀在哪一步"：问 `7/2` 与 `7/2.0` 差在哪、`8/3` 印几。课本 **p61**「两个整数相除的结果为整数，如 5/3 的结果值为 1，舍去小数部分」
- 🔴 约 09-16~09-17 复检（n=1~2 封顶不算掌握）：标识符 6/6、字节↔位与单位阶梯 2/2、赋值覆盖
- ✅ 已过关：最小框架默写 20/20 · #C002 销账 · 花括号作用域 3/3 · 变量命名 `grade` · **`printf("%d",'\n')`→10** · **`printf("%c",65)`→A**

## 学到哪了

进度条 **12%**（过 3 格：最小框架、赋值覆盖、**`a++`/`++a` 落地时机**）。讲义已沉淀 `docs/posts/computer/notes/1.3a-变量与赋值-零基础.md`。

**📍 章节位置（09-14 晚间）**：1.1 概述 · 1.2 数据的存储与运算 · **1.3 顺序程序设计 —— 全部过完**（五节逐节讲 + 两轮共 8 题实测）。**下一站：1.4 选择结构程序设计（`if`）**，但**先把上面 🔴 三笔欠账清掉再开新课**。
1.3 五节逐节记账（**已过的别重教**）：

| 1.3 的小节 | 状态 |
|:---|:---|
| 一、C 语句分类 | ✅ 09-14 教过（五类：控制 / 函数调用 / 表达式 / 空 / 复合）。⚠️ 他答对「表达式语句」却自报"没教过"→ 说明**只扫一眼没入脑**，此类纯背项考前再刷即可，不占格子 |
| **二、printf 输出函数** | ✅ **09-14 全部教完**：坑位数 / 普通字符原样输出 / 多客人被扔（gcc 警告）+ `%5d` `%-5d` `%.2f` `%c` `%s` 全家福。gcc 实测两条关键：**宽度 = 最少占几格，不够补空格，超了原样印不砍**（`%5d`+42 → `···42`；`%5d`+1234567 → `1234567`）。⚠️ **#C004 同日 n=2 不算稳，约 09-16 复检** |
| 三、scanf 与 `&` | ✅ 09-14 教过。课本 **p98**「`&a` 指变量 a 在内存中的地址」+ **p99**「格式控制后面应当是变量**地址**，不是变量名」。⚠️ 他首考理由是**循环论证**（"因为这是 scanf 语句"）→ 已补讲**方向对比**：printf 是**读出来**（不用 &）、scanf 是**写进去**（必须给门牌号才能写）。**`&` 方向下次必复检** |
| 四、getchar / putchar + 吃换行符 | ✅ **09-14 晚间重讲通过（但只隔一天，仍列欠账①）**。首版一次塞 6 层 → 他自报「看不下去」；**推倒只留「点餐台排队票」一个画面**重讲 → 答对 `a=5 / ch=回车` ✅，终两题 `printf("%d",'\n')`→**10** ✅、`printf("%c",65)`→**A** ✅。gcc 实测 `ch` 收到编号 **10 = 换行符**；修法**只记一种**：`scanf(" %c", &ch)`（那格空格 = 清洁工命令：先把空白破烂全扫掉）。⚠️ **下次必须换同型**——原题答案已进讲义 1.3b |
| 五、数据类型转换（整数除法） | ✅ **09-14 销掉昨日雷**：课本 p61「两整数相除结果为整数」。实测 `9/4→2`、**`1/2→0`**、`3*5/2→7`、`7/2.0→3.5`。他 `int i = 1/2;` 答 **0 ✅** —— 此题**根本没有 printf**，正好自证"砍刀在除法、不在 `%d`" |

> 📊 **09-14「一节一课」节奏首判 → 达标保留**：1.3 剩余四节一次讲完 + 8 题小测，**数字层面 5/5 全对**（①②③④⑥），扣在**理由**上两处（②仍说"打印时砍掉"、⑦循环论证）+ ⑤要求讲解 + ⑧主动留白 ≈ **81% ≥ 60%** → **不退回一次一概念，「一节一课」继续**。
> 🎯 **本日最大变化不在分数**：他零基础以来第一次**主动连环提问**（问 `%5d` 到底几个空格、问 `&` 是不是"读取内存"、要求先解释 printf/scanf/getchar 这些英文单词、⑧ 明说"没通透先讲完我再答"）。**这是从"被动接题"转向"主动要解释"的拐点，比多讲一节重要。**
> ⚠️ 同时暴露一条判卷新规矩：**他会用"答对数字"掩盖"理由没懂"**（②⑦两处），故"讲理由"要求今后**每题硬性必填**，只交数字或理由循环 = 一律按未过。

> 🔴🔴 **09-14 当晚判据漏洞（比上面那条更要命，写给下一个教练）**：上面那个"81% ≥60% 所以节奏保留"的判据**当场被证伪**。他随即回了一句 **「这一部分还是看不下去，讲得简单点」** —— 8 题数字全对，**却完全没听懂其中一节**。
> **结论：做题正确率抓不住"听不懂"。** 数字对，可能只是套对了模板（他 ② 就是这样：答对 2，理由仍是"打印时砍掉"）。
> **今后硬性加一道检查**：每讲完一节，**必须主动问一句「这一节里有没有哪句话你看不懂？」**，并明确告诉他"看不懂就说，不用做题证明"。**只有他亲口说不懂，才算这一节的真实反馈。**
> 🧾 第五节失败的具体原因（我的写法问题，不是他的理解力）：那段一口气塞了 **6 层** —— ASCII 编号 10/88 + `%c` 不挑字符 + `%d` 遇空格结束 + 课本 p99/p100 双引证 + 两种修法 + "桌上留东西"比喻。**比喻和术语混排 = 零基础抓不到主线。** 重讲版已改为：**一次只留一个画面（传送带上剩一个空盘子），ASCII 编号全删，修法只留一种，页码撤到备注**。
> ✅ 同日亮点（记录以防误判他"没懂"）：他自己推出疑问「那 `getchar` 也要 `&` 吗？」—— 说明"往里写要门牌号"这个模型他**已在主动迁移使用**。纠偏结论：**`ch = getchar();` 不要 `&`**（字符放在 `=` 左边，赋值自己会找盒子；`&` 是给"没有等号、只有 scanf 自己往里塞"的情况准备的）。gcc 实测 `getchar(&ch);` 直接报 `too many arguments to function 'getchar'; expected 0, have 1`。

本话题的讲法：**一课一个概念**，推进顺序 = **整数除法截断（回炉）→ scanf 的 `&` → scanf 吃换行符 → printf 宽度对齐**。

> 🔄 **09-14 节奏改判（用户拍板，覆盖上面的"一次 1 概念"默认）**：用户主动要求「一次把 1.3 剩下的全讲完」，理由是自己 1.1/1.2 已通读过一遍、单点推进太慢，觉得"每天学这么少"。
> **新默认 = 一节一课**：一次讲完 1.3 剩余 4 节，**但每节末尾配 1-2 道立刻作答的题**，判卷仍只认过程；「没教过的不出题」这条不破。
> ⚠️ **风险明写在纸上**：09-13 一次灌 9 个考点 → 紧接着三题全错，那就是同一种做法。**若本次课后小测正确率 <60%，自动退回「一次 1 概念」，不再商量。**
> 📌 同时向用户澄清了口径：**"每天 4-5 小时"是学习时长上限，不是考点数量上限**；他今天实际用时已约 1.5h（做题 12 道），"感觉少"是因为按**考点格子**记账、格子攒得慢 —— 攒慢的原因是**假掌握要重做**（#C001/#C002/#C003 三笔都是首答错、当天回炉才对）。
> ⚠️ 用户粘贴的整份笔记是**弹药库不是教案**。
> 课本锚点：**p94**「`printf(格式控制, 输出表列)`；格式声明由 `%` 开始，其余是原样输出的普通字符」。
> ⚠️ 1.1/1.2 里**混着计算机基础理论**（五大件、字节↔位在 `0.0-计算机基础理论.md`）—— 那部分欠账去「继续 计算机基础」话题收，**别在本话题重复考**。
下一站跟着看板走，**不要自己跳去数据结构**（那是二轮任务）。

## 本话题禁止项

- ❌ 不改门户 / 脚本 / 背词本（`kind:"tool"` 一律不算学习量，封刀戒律已破 3 次）
- ❌ 不复制课本里的代码（抽取结果右花括号缺 142 个）
- ❌ 不说"这个很简单"—— 他是零基础
