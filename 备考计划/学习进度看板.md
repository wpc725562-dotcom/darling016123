# 📊 学习进度看板（AI 交接单一事实源）

> 🤖 **本文件是 AI 教练跨会话交接的核心文件**——每次学习会话结束时由教练更新，新会话开始时读取。
> 👤 **给你**：也可以手动编辑任何区块，AI 以下次会话读取到的最新内容为准。
> 🔄 **更新协议**：每次学习会话结束前，AI 必须刷新「上次会话」「当前状态」「本周任务」三区，其余区按需更新。`last_updated` 是新会话判断信息新鲜度的依据。

**last_updated**: 2026-09-23 04:30（02 更新：🌙 **09-23 凌晨场结算（01:40 → 04:20）—— C 语言 1.5-⑤ 嵌套循环两格过 + 英语第 3 篇 + 一次教学法大修**。📐 **1.5-⑤ 嵌套循环**：课本 **书 p140（物理 p157）**。开课前取证两条 —— ① `textbook.mjs 嵌套` 命中物理 p157，**页脚「140」实测可见**（顺带验证「书页 = 物理 − 17」铁律）；② 🔴 **真题分量实查（诚实说）**：`grep 嵌套` 四年真卷 → **严格意义的「循环套循环」没直接出过题**，「嵌套」二字只在 **2023 一道判断题**「C 程序可以嵌套调用。（ ）」= **函数**嵌套；**但**三年 10 分计算题考的都是「**循环体内套选择**」（2021 第41题 `for` 套 `switch` / 2023 第41题 `for` 套 `if`+`continue` / 2024 第41题 `for` 套 `if`）⇒ **嵌套循环不是直接考点，是那 30 分的地基**。抓手沿用第②课「**`for` 只管紧跟它的那一条语句**」；画面「外层走 1 步、内层跑完整圈」；**gcc 凭据 6 个文件全实跑**（nest1–9：内层变量同名只印 1 行 / 内层 break 外层照跑 3 圈 / 换行三位置对照）。**格 A 同型题**（`for(i..4)` 套 `for(j..6)`）首答错（`j=8`、行数答 5）→ 讲清「`j++` 跟在循环体后面，判断失败时没有体就没有 `j++`」→ 重考 **4 项全对**（`i=5 j=7 n=24`，gcc `nest8.c`）；**格 B 变式**（内层上限 `5 - i`）首答「每行 3 个」→ 断点定位为「**只代入了 `i=2` 那一行**（取样偏差）」→ 给逐行填空表 → **全对**（4/3/2/1、共 10 个、`i=5 j=2`，gcc `nest9.c`）。**两格均记 🟡**（同一场次未过夜）。🔴🔴🔴 **本轮最大事件：学生 03:20 提出元问题「我对自己在解题思路上的想法是有价值的，但为什么我仍不理解这些知识点？」→ 触发一次教学法大修**。复盘发现：**他两次卡点（`j++` 何时跑 / `\\n` 归谁管）都靠同一条已学规则解掉，而那条规则是我在他问出口之后才挂上去的** ⇒ 根因 = **我教的是「操作步骤」，他缺的是「结构关系」**（这也解释了 09-14 就出现的「数字能对、理由说不出」老病）。已沉淀 **`教练手册-教学纪律.md` §2.0.5 讲解结构六条**（① 先讲「谁管谁」再讲「怎么算」② 讲新东西前先问「这跟你学过的哪条规则像」③ **同一知识点禁止连换 3 版以上讲法** ④ 讲法不通**先问「上一版哪一步断了」**不要直接给新版 ⑤ **判卷后必须明说「你这次的思路哪里是对的」** ⑥ 定义放到例子之后）+ **`教练复盘档案.md` 2026-09-23 整节**。⭐ **§2.0.5 首次实测有效**：嵌套循环**一版不换**、只问断点 + 给填表脚手架 → **变式一次就通**；对比同一晚**进制转换换了 4 版**（权重+除2取余 → 格子 → 一张表 → 找零钱硬币）仍没通、学生主动跳过。📖 **英语语法填空第 3 篇**（2015 全国II卷·土坯房）**5/10**（`built` `the` `using` `slowly` `goes`）—— 🔑 **最重要发现：他的「词性直觉」是对的，缺的是「词形表」**（第 3 空他知道 `their` 后要名词、第 9 空他知道修饰名词要用形容词，**只是不知道 ability / natural 怎么写**）⇒ **不是「不懂语法」，是「句法直觉有了、词形变化没学过」= 纯知识缺口**。错误分类：**知识缺口 5 / 推理错 1**（第 1 空把「过去分词短语作定语」当成「谓语被动」，真谓语是 `are admired` → **下一课：非谓语动词**）。⚠️ **09-21 那次的 3 个错也全是知识缺口** ⇒ 两次都是知识缺口为主 ⇒ **行动方向 = 补词形转换，不是再讲语法**。📊 日志 63 → **65 条**（C语言 39 / 英语 8）。⏭ **1.5 只剩最后一块：1.5-⑥ 综合举例**（课本 5.5「用 `break` 提前退出循环」+ 例 5.6，物理 p158 = 书 p141）。⚠️ **Anki 建卡未做** —— 收工时 **AnkiConnect 离线（Anki 未开）**，E56/E58/英语词形三张卡待补）｜此前 2026-09-22 22:20（02 更新：🔧 **口径修正：「计算机」这个日志科目 = 计算机基础，不是 C 语言**。起因：darling 追问「什么叫做突然多了计算机基础」。查实 —— ① 它**不是新科目**，是四个学科之一（`开新话题/计算机基础.md`），09-13 就学过 4 条；② 日志 `subject` 字段写作「**计算机**」而非「计算机基础」，按字面精确匹配会**查不到** → 我此前在 `可行性评估` §2 误记「日志 0 条」，与同文件 §3「09-13 计算机 4 条新内容」**自相矛盾**；§3.3 断档表更把同一科**列了两行**（「计算机」9 天 / 「计算机基础」无数据）。**已全部修正**（`可行性评估` 315 行，表格校验通过）。③ **真题取证（这是重点）**：`历年真题/真题txt/计算机基础与程序设计/` 有 **2021–2024 四年真卷**，其中进制换算 **2021 第 18 题**（选择 3 分：十进制 630 → 二进制 = `1001110110`）、**2022 第 39 题**（**简答 10 分**：十进制 2022 → 二进制 `11111100110` + 十六进制 `7E6`）、字节题 **2023 第 31 题**（填空 4 分：`char` 占 **1** 字节）—— **正好就是今天块 A 要学/复检的内容**。⚠️ **教训已写进 skill**：查「某科有没有落账」**禁止按科目名精确匹配**，必须先跑 `subject` 取值分布再人工归类）｜此前 2026-09-22 02:58（02 更新：📇 **补建 2015 篇卡片 17 张** —— **学生自己想起**「今天学习的语法填空卡片没更新到 anki 上」。走项目既有 **AnkiConnect 直写通道**（`http://127.0.0.1:8765`，笔记类型 `问答题` / 字段 `正面`·`背面`；`createDeck → addNotes → findCards` 三件套 + `notesInfo` 回读抽查 4 张实际内容）：**语法牌组 6 → 15 张、生词牌组 15 → 23 张**，17 张全部写入、**0 重复**。卡片依据 09-21 的 **3 个错题**（`arriving`→`arrived` / `after`→`before` / `for`→`by`，每张正面都带「🔴 你填了 X —— 为什么错？」）+ 他主动问的**分句谓语** + 我讲过的**非谓语作定语**（`conducted by` 过去分词被动 / `people living` 现在分词主动）与**定语从句**（`that/which`）+ **8 张生词**（choking smog / limestone / dawn / skipped / head straight for / scenery / destination / getaway）。TSV 落盘：`英语-语法填空2015-语法-cards.tsv` + `英语-语法填空2015-生词-cards.tsv`。⚠️ **这是第 6 次「做完但没落账」** —— 前 5 次含 09-19 英语 2014 篇漏记；**本次的机制漏洞是「做题 → 建卡」这一步不在收工四件事里**，已补进 skill。📊 日志 62 → **63 条**）｜此前 2026-09-22 01:25（02 更新：📊 **可行性评估交付 —— 结论：来得及，且富余 73 天**。逐条核对 61 条日志后**修正一处既有统计错误**：`高强度学习方案-2026-09-20.md` 记的「推进日密度 **23%**（3/13 天）」是**低估**，漏算了 **09-09**（高数 20 分钟扫盲微课 + 极限 4 题）、**09-10**（讲义 1.11 抓大头 + C 语言第三课提前插队）、**09-13**（计算机字节与位 / 单位阶梯 / 硬件五大件 4 条新内容）→ 重判后：跨度 09-07~09-22 **16 天**、有记录 **12 天**（出勤 **75%**）、**推进日 8 天 = 密度 50%**（正好是方案 §5.1 的目标值）。**可行性**：29 模块 ÷ 0.667 速率 = 43.5 推进日 ÷ 0.5 密度 = **87 天** vs 倒计时 **160 天** → **富余 73 天**；🎯 **临界速率 = 0.3625 模块/推进日**（160 × 50% = 80 推进日，29 ÷ 80），实测 0.667 是它的 **1.84 倍**。🔴 **真问题不在密度、在 Anki**：直读 `deck_config` 字段 10 = **200**（复习每日上限）—— **这就是学生看到的「200 张到期」**，而**真实到期 465 张**（复习 457 + 学习中 8）→ **每天被拦下 265 张顺延到明天**，明天再叠新的 → 永远做不完。**不是懒，是设置问题**。今日按钮分布：Hard **232（57.7%）**、Good 127、Easy 31、Again 12；全库 6183 次：Hard 2588（**41.9%**）、Good 1810、Easy 1243、Again 542 —— **Hard 的语义是「请把下次间隔压短」**，按 42% 的比例按 = 每天亲手把间隔压回去。另：**1053 张高频词新卡从未学过**（新卡上限 40/天，过一遍要 26 天）。🔴 **单科断档**：**计算机断 9 天**（09-13 至今），是专业分（200 分占 40%）却拖最久 → 建议块 B 直接换计算机。📄 交付两份文件：`可行性评估与提效建议-2026-09-22.md`（306 行）+ `Anki-多端同步与升级-2026-09-22.md`（217 行）。📊 日志 60 → **62 条**。⚠️ **Anki 三件事（升级 / 建账户 / 同步手机）属对外动作，已列清单等确认，未执行** —— 安装包已下载、数据库已备份 3 文件）｜此前 2026-09-22 01:05（02 更新：🌙 **09-21 全天结算（16:40 → 01:01，跨 0 点收尾）—— 三块必做全部达标**。📐 **块 A 高数 0/0 型约分第一课 达标**（三题全对 4/6/2、答案刻意错开、node 数值表三题全对账；⭐ 关口「为什么能约」一次说对 —— 首答「说不清楚」→ 讲「**趋近 0 ≠ 等于 0**」→ 他答「无限趋近于0 不等于0 可以除」；**关口设计可复用**：把矛盾摆到他面前，而不是抽象问「为什么」）→ 记 🟡。📖 **块 B 英语 达标**：语法填空 **2015 全国I卷 7/10**（干净基线，3 错全是「知识缺口」不是粗心）；教了**分句谓语**（「一个句子只有一个谓语」管的是**一个分句**，主句从句各有各的谓语 → `when I ___` 填 `arrived`）；⭐ 他**主动提元问题**「没学过语法就能做语法题吗」+ **自陈「长难句还是要学习」**（需求已记，优先级高于教练排期）。🔁 **块 C C 语言跨日复检 4 格：⑫⑬⑮ 升 🟢，⑭ `break` 三考才过**（详见 C 语言行）。🔴 **今日最大事件：第 5 次漏账事故查实** —— 英语「163 篇零使用」是误判，**学生是对的**；**Anki 已列为第二事实源**（详见英语行与 `2026-09-21.md`）。📊 日志 55 → **60 条**（C语言 38 / 高数 6 / 英语 5 / 工具 4 / 计算机 4 / 综合 3）｜此前 2026-09-21 16:25（02 更新：🔴 **查实第 5 次漏登记事故 —— 英语「163 篇零使用」是误判，学生是对的、我是错的**。取证过程：学生 09-21 说「前几天在你这里做过 2014 语法填空，Anki 里有记录」→ 02 先查四处**全部落空**（学习日志 55 条里英语只有 3 条 Anki / `资料/英语专项/` 23 个文件无任何进度文件 / git log 无相关提交 / 历史对话检索 0 结果）→ 按学生指路**直读 Anki `collection.anki2`** 才查到：牌组「专升本英语-语法」（6 张）与「专升本英语-写题陌生词汇」（15 张）**均创建于 2026-09-19 19:34**；复习记录 英语-语法 09-19 20:10–20:11（5 次）+ 09-20 01:32–01:33（5 次）、写题词汇 09-20 01:29–01:31（24 次）；**卡片内容 100% 来自 2014 全国I卷第 1 篇**（语法卡例子 `Just be ___ (patience)` / `___ (amaze) stories` / `the ___ (change) are gradual` / `the water in the river is ___ (clean) than ever` / `it ___ (actual) caught fire`；生词卡 Cuyahoga / pollution / industrial / terrible / along / caught fire / impossible situation / credit card / hard work paid off / unimaginable / amazing stories / gradual / require / even / instant transformation）。**已补记日志 1 条（第 56 行，date=2026-09-19）→ 英语修正为 4 条 / 最近 09-19 / 断 1 天**（原「断 8 天」作废）；看板英语行同步修正。⚠️ **教训：Anki 是第二事实源，收工核账必须一并读 revlog** —— 只看 `学习日志.jsonl` 会漏掉「做了但没落账」的部分。⚠️ 责任在教练侧：收工四件事的「学习日志追加」没落全）｜📐 **块 A · 高数 $\frac{0}{0}$ 型约分第一课 完成达标** —— 三题全对（$\lim_{x\to2}\frac{x^2-4}{x-2}=4$ / $\lim_{x\to3}\frac{x^2-9}{x-3}=6$ / $\lim_{x\to1}\frac{x^2-1}{x-1}=2$，答案刻意错开，node 数值表三题全部对账一致）；⭐ **今天的关口（为什么能约）一次说对**：首答「说不清楚」（诚实）→ 给「$x-2$ 逐格表」讲「**趋近 0 ≠ 等于 0**」→ 他答「**无限趋近于0 不等于0 可以除**」✅。**关口设计值得复用**：不抽象问「为什么」，而是**把矛盾摆到他面前**（「你说了『分母为 0』，但你除以了 $x-3$，两句不可能同时成立」）。记 **🟡**（今天独立做对，明天跨日复检过了才 🟢）。⚠️ 纪律：备用题 $\lim_{x\to2}\frac{x^2-5x+6}{x-2}$ **没出** —— 需十字相乘而十字相乘未教，按「没教过的不出题」搁置（**第 6 次差点踩，这次守住了**）。⚠️ 术语更正：学生说「十字相乘」，$x^2-4$ 用的是**平方差**）｜此前 2026-09-21 02:40（02 更新：🌙 **1.5 循环结构连下三课（第三/四/五课）** —— ① **第三课 `do-while` 判卷一次通过**（`int i=5; do{printf("%d",i); i++;} while(i<3);` → 答「印 5，i++ 为 6，进 do-while 是先输出一遍」= **「先执行、后判断」核心规则自己说出来**，gcc 对账 `do2.c`）；② **第四课 `for` 开讲 → 撞上「末尾分号」大坑，卡了 3 轮才过** —— 他先说「不懂为什么 F2 里 `s2 = 100`」「s 没加入循环」，两轮答错数字（「4 次」、「循环 4 次执行 3 次」），根因是**同一段代码里的三个数分不清谁是谁**（循环体次数 3 / 循环变量最终值 4 / 循环外语句次数 1）；改用「**复读机比喻 + 逐格执行表 + 三步机械法**」后**无分号版三问全对（3/4/3）**，随后**迁移题全对（4/5/10 —— 换数字、换增量、换有无分号三个维度同时变）**；③ **第五课 `break`/`continue` 首题全对（`12 6`）**，且 `s=12` 是他**自己推出来的**（报出中间值序列 `1→3→7→12` + 被加进去的 `i` 值 `1245`）。📌 **新增易错点 4 条**：**E52**（for 末尾多一个分号，大括号掉到循环外）、**E53**（把循环变量的最终值当成语句执行次数）、**E54**（while 里 continue 写在 i++ 之前 → 卡死）、**E55**（break 出去后循环变量的值，不是多走一步）→ 易错点地图现为 **7 类 55 条 / 🔥32**。📌 **gcc 凭据已持久化**：`编程技能/零基础教程/gcc-1.5循环/`（for1-4.c / break1-2.c + README.md 写明每个实验的源码用途与预期输出）。⚠️ **本轮 4 格全部 🟡**（均为同一场次内做对，**未过夜**，按口径 v2 不升 🟢）→ 覆盖率 **11/25 → 15/25 = 60%**、已过率 **11/11 → 11/15 = 73%**。⏭ 下一课：**嵌套循环 + 1.5 综合举例**。⚠️ 教练侧自记：本轮教学法两处修正 —— ① **并排对照图对他连续失效两次**（对照图假设他已懂单侧），改用「逐格执行表」一次见效；② 他明确反馈「gcc 实测题目没给全给我」→ **今后凭据必须给全源码**，只给结果表他无法自证复现）｜此前 2026-09-20 14:45（02 更新：🎉 **1.4 选择结构程序设计 全部完结 + 跨日复检三格同时升 🟢** —— ① T8（条件表达式 `x>y?x:y`）答 **`9`** ✅ —— **`?:` 是 03:38 讲的、14:27 答的，中间过了一夜 → 长时记忆验证，不是短时记忆**；② 4.4 闰年题（`leap` 标志变量）补讲后 **2/2 全对**；③ **跨日复检 3/3 全对**（R1 `else` 配对 → `[Y]`、R2 逻辑求值 → `0`、R3 `switch` 穿透 → `[C][E][D]`，全部 gcc 对账 `recover.c`）→ **第四课 / 第五课 / `switch` 三格同时升 🟢**，进度条已过率 **8/11 → 11/11 = 100%**、覆盖率 11/25 = 44%。⚠️ R1 曾因「理由没报出 `else` 配哪个 `if`」被追问，他答 **B（内层）** + 「**`else` 靠近谁跟谁**」= 第四课核心规则自己说出来了。📌 **新建讲义 `1.4b-嵌套if与switch-零基础.md`**（关系运算符全表 / 嵌套 `if` / 逻辑运算符 / `switch` / `?:`，含他点名要记的优先级阶梯「算术 > 关系 > 赋值」，自检 8 题全 gcc 实测）。⚠️ 本轮教练侧记录 **第 4、5 次「没教过就出题」**（`!=`+`%` 未教、`leap` 标志变量未教）—— 见弱项画像。此前 01:52：**4.3 `switch` 收口 → 1.4 第六课完结；进度条新增第 11 格** —— T6 答 **`[D][H]`** ✅ 理由完整（「走 `default` 路线因为没有 `break` 往下滑到 `[H]` 遇到 `break` 停止」），**正是上一轮 T5 漏掉的那一步，说明「`default` 也是标点、会穿透」已内化**。4.3 全程：T1 不计分（`default` 未教）、T2/T3/T4/T6 全对、T5 漏穿透一次。进度条 **10/25 → 11/25 = 44%**、已过率 **8/10 → 8/11 = 73%**（🟡 三格：第四课 / 第五课 / `switch`，均因**未过夜**不升 🟢）。⭐ 本轮他**自主设计了一个对照实验**（问「`default` 与 `case 7` 对调后 `case 4` 还会穿透到 `default` 吗」）→ gcc 实测**不会**（`[R][S]`，被 `case 7` 的 `break` 截断），并由此得到本课最硬的结论：**穿透跟的是「代码里写的物理顺序」，不是 `case` 数值大小顺序**（`case` 倒着写 `7-4-1`，`n=7` → `[S][R][P]`）。⚠️ 进行中：**4.4 程序综合举例（例 4.5 闰年）+ 4.5 条件表达式 `?:`** —— 1.4 最后两块。此前 01:45：**4.3 `switch` 收尾三题 + 查出教练第 3 次「没教过就出题」** —— T2（多 `case` 共用 → `X`）/ T3（穿透+`break` → `CD`）全对；T1（穿透）答 `BC`、漏 `D`，**判不计分**，因为题面里的 **`default` 从没正式讲过**（他主动提问「`default` 还没有详细介绍……和 `else` 差不多是吗」= **反馈成立**）。已补讲 `default` + gcc 七路实测（`default2.c`），核心区别：**`else` 是条件的一部分、不穿透；`default` 是标号、会穿透**。重考题已出（`t45.c`：`n=4` → `[R][T]`、`n=9` → `[T][P]`）待答。⚠️ 用户拍板「今天直接把 1.4 学完」→ 剩余 **4.4 综合举例 + 4.5 条件表达式 `?:`** 两轮内收口。此前 01:35：**09-20 首检 4/4 全对 + 1.4 第六课 `switch` 开讲** —— ① 首检 A 组（`else` 配对换变量 + 混用大括号）2/2、B 组（逻辑「求值」形式，答案必须 0/1）2/2，**🔴 流程纪律观察位首次清账**（他主动报全两条配对关系「走 if 外的 else / 走 if 里的 else」，未追问）；② `switch` 开讲（书 p117 / 物理 p134）—— 画面「电梯直达」，首题（case 顺序打乱版 `x=2`）答 **`[R]`** 正确且理由说出「按值找格子」。⚠️ **穿透题故意留到明天**（刚讲完 10 分钟考 = 短时记忆，过夜后考 = 长时记忆）。⚠️ **本场距第四课仅约 5 小时、未过夜 → 按自定判据「跨日 = 过了一夜」仍判同一场次，第四课/第五课均不升 🟢**，进度条维持 10/25、8/10。此前 01:15：**新增 [[自由作息与考前切换方案-2026-09-20]]** —— 用户日常凌晨 3–4 点睡、要求作息不固定 → 自由期改 **T0 锚点式**（结构固定、时刻随你，红线仅「起床浮动 ≤2h」），考前 4 周按每周前移 1–1.5h 切到 23:00 就寝。⚠️ **同时订正两处事实**：① **4 科 500 分**（计算机专业综合课 **200 分**，占 40%）不是 5 科；② **2026 年考试在 3 月 28–29 日** → 2027 年大概率 3 月最后一个周末，**比本看板基准晚约 26 天**（基准不改）。此前 01:05：**新增 [[高强度学习方案-2026-09-20]]** —— 用户要求进入高强度模式。方案的靶心是一个实测数字：**推进日密度 23%**（13 天里只有 3 天在推进新内容）→ 目标 **50%**。配套给出四阶段排期（每阶段标了最低密度门槛）、每日五块结构、干扰控制四条、五个手工可记指标。⚠️ 方案本身**不计学习量**，生效标志是密度真的上去。此前 00:55：**C 语言 1.4 第五课 逻辑运算符 `&&`/`\|\|`/`!` 讲完** —— 首题撞 #C007 后订正，全课 7 问 6 对（真题原型两问全对）。新增 🟡 第 10 格；进度条 9/25 → **10/25**、已过率 8/9 → **8/10 = 80%**。⚠️ **本场收尾已跨 0 点（09-20 00:51），但距第四课仅 4.5 小时，按项目「同日连做不算稳」精神判为同一场次，第四课不升 🟢** —— 真正的跨日复检放 09-20 白天。此前 20:55：**1.4 第五课 `&&` 开讲，首题撞新洞 → 已立 #C007** —— 他答「不打印」，逻辑推理全对（b=0 假 / a>0 真 / 一个假就整体假），**错在把 `printf("%d", 表达式)` 当成了 `if`**。这是新概念「**表达式有值**」缺失，且**真题几乎每年必考**（2021 求 `!a||b` 的值、2022 求 `(a==6)||(a!=6)` 的值、2023 `a&&b||c`）。已教一个概念 + gcc 五路对照，重出对照两问。此前 20:25：**C 语言块① 完成 + 1.4 第四课「嵌套 `if` 与 `else` 配对」讲完并当场考过** —— 首考 3 问 + 变式 3 问 **6/6 全对**（gcc 两路对账），新增 🟡 第 9 格；进度条 8/25 → **9/25**、已过率 8/8 → **8/9**（🟡 按口径 v2 计入分母不计入分子）。⚠️ 本课前教练**第 2 次犯「没教过的不出题」**，被用户当场指出，补讲后重考。另：判据侧两处订正 —— `else if(a>3)` 是弱题、`lim(x→0) 1/x²` 是无效备选题）
**📅 最后一条学习落账**: **2026-09-22**（综合 · 可行性评估 + Anki 诊断，见 `学习日志.jsonl` 第 62 行）→ ✅ 连续有记录（09-19 / 09-20 / 09-21 / 09-22）
> ⚠️ **本行此前长期停在 2026-09-19，属陈旧值** —— 实际 09-20 / 09-21 / 09-22 均有落账，2026-09-22 已修正。
> **为什么单列这个字段**：原来看板只有 `last_updated`，它把两个概念混在一起 ——
> 「文档改没改」和「学习数据新不新」。后果是**连续 3 天零学习落账，进度条照常显示 32%，
> 没有任何提示**，要靠人翻日志才发现。
> 现在分开：`last_updated` = 文档更新时间；`最后一条学习落账` = **数据新鲜度**。
> **缺测期间进度条冻结（不涨不跌）** —— 缺测 ≠ 0，也 ≠ 有进展。
> **解除方式**：完成落账四件事（见 [每日学习计划-2026-09-19](./每日学习计划-2026-09-19.md)）。

**考试倒计时**: 剩余 **160 天 ≈ 22.9 周**（按 2027-03-01 计；09-22 更新）
> 📐 **2026-09-22 可行性测算**：所需 43.5 推进日 ÷ 实测密度 50% = **87 天** → **富余 73 天**。
> 🎯 **临界速率 0.3625 模块/推进日**（低于此值才来不及；实测 0.667）。详见 [可行性评估与提效建议-2026-09-22](./可行性评估与提效建议-2026-09-22.md)
> ⚠️ **2026-09-20 补充（查官方原文后）**：**2026 年考试实际在 3 月 28–29 日**（周六日），**4 科 500 分** —— 政治理论 / 英语 / 高等数学各 **100 分 × 120 分钟**，计算机**专业综合课 200 分 × 150 分钟**（占 40%）。
> → **2027 年大概率在 3 月最后一个周末（03-27/28）**，比本行基准**晚约 26 天**。**基准不改**（保守无害、且改动会波及大量文件），但规划时要知道**实际可能多出约 26 天缓冲**。来源：广东省教育考试院 2026 年招生规定（深圳本地宝 2025-11-20 转载全文）。
**🎯 目标分**: **410 / 500**（= 82%）。⚠️ **410 是"刚好卡门槛"，不是"稳"** —— 2025 计算机类公办实际投档最低分 **409**、最低排位 278、公办录取率 **≈3.8%**（271/7071）。**拆解与差距分析见 [目标分拆解-410.md](./目标分拆解-410.md)**

---

## 🎯 一句话当前状态

**⚠️ 09-08~09-09 大部分时间仍在磨工具（门户/脚本），看板原警告的「完善工具式伪勤奋」确实发生了。**
**但 09-09 出现了第一次真实作答**：C 语言最小框架 0/20 → **20/20（默写全对）**；高数 4 题 **20/40**。唯一缺口非常明确——**「会读符号、不会算结果」**：题 2 只把条件翻译成人话就停了（没算出值），题 4 判断对但说不出原因（按置信度封顶记 0.5，不算掌握）。
弹药已上膛：门户「今日刷题」= KaTeX 真数学 + 读题教练人话讲解 + 答案二次折叠逼回想 + 全题库加权随机；另新增「今日三步」面板与 Anki 真实进度面板（英语显式降为保鲜轨）。
**09-09 深夜～09-10 凌晨（第二轮实测）：那 3 道欠交题已全部交回且全对**，加上补做的 ④⑤，**高数 6 题 100/100**（同考点上一轮是 50%）——「会读符号、不会算结果」这个缺口**已闭合**。题 ③ 的理由是他自己的反问句（"2x 无限趋近于无穷大怎么会等于 0"），不是背的。

**但 C 语言立刻撞新墙，且根子在教练身上**：人肉编译 `for(i=1;i<=4;i++) s=s+i;` 他答 **4**（正确 10）——把计数器 `i` 的最后值当成累计器 `s`，还以为循环只跑 1 轮。他当场指出「你是不是忘了我都是零基础」，**这条反馈成立**：教练只教了三行框架就直接抛 for 循环，变量/赋值根本没教。→ 立为教学纪律：**一次只给一个新概念，没教过的不出题。**

**🟢 最新（09-14 晚间）：C 语言「1.3 顺序程序设计」整节讲完并实测。** 但这一场真正的产出不是分数，是**抓出了判据的洞**——8 题数字 5/5 全对（81%），他仍当场说「**这一部分还是看不下去**」→ **正确率抓不住"听不懂"**。已把节奏改判为「每节讲完先问『哪句话看不懂』，答了才进下一节」，并把失败的那节推倒、**只留一个画面**重讲后他随即答对。剩 3 笔欠账（吃换行符 / `%5d` 默写 / `&` 方向）下次开课首检，随后进 **1.4 `if`**。

**🟢 最新（09-15 下午）：C 语言「1.4 选择结构」连下三课，全部 gcc 实测。** 开场先清完 09-14 三笔欠账（吃换行符换同型 `getchar` 版答对 `a=2.5/p=回车/q=X`；`%5d` 逐字符最终 `<···9>`=**6 字符**全对；`&` 方向他说出「printf 向外取出来不用 `&`」）。随后开 1.4：① **门卫只认 0**（只有 0 是假，-8/100 全为真）—— 3 题全对且**自主推出**"0 代表假所以不打印"；② **`if-else` 两条路 + `=` 塞 vs `==` 问**（`if(a=3)` 会把 a 改成 3 且判真，gcc 实测，程序不报错）；③ **`else if` 阶梯第一个说"是"就进屋、门槛必须从大到小**（把 score=85 的门倒序排 → 正确 `[B]`、倒序 `[C]`，他看到实测后自己复述"顺序错了第一道门就通过了"）。**判分 9 题 8 对**，唯一真错见 #C006。🔴 另当场查明并订正**课本页码系统偏差：纸质书页码 = txt 物理页号 − 17**（逐页读页脚实测恒定；`textbook.mjs --toc` 自述"差约 13 页"是错的）→ 1.3b 讲义、开场卡、新讲义 1.4a 全部按书页码重标。**沉淀**：新建 `docs/posts/computer/notes/1.4a-选择结构-if语句-零基础.md`（12665 B，已登记侧栏 + notes 索引，自检 8 题答案逐条 gcc 实测）。🆕 **用户拍板新节奏（覆盖旧默认）：「一天可以交很多新概念，但一次交一个概念」** → 一概念 → 一题 → 过了才给下一个，禁止一次铺 3 题。

**🔴 假掌握警报 → 09-10 凌晨已解除**：先「有表格填 `1→3→6→10`」全对、「撤表格做 `a=a+2; a=a*2;`」答 6（假掌握）；随后**合上讲义无表格重做答 10 ✅**，同型新题 `x=10; x=x-4; x=x+1; x=x*2;` 答 **14 ✅**（10→6→7→14）→ **同型连对 2 次，「赋值覆盖 / `=` 从右往左」这一格真过关**（#C001 复习记录已签）。

**🆕 但立刻撞上一个更该修的病：答案从上一题借。** ③ `for(i=1;i<=3;i++) s=s+i;` 答 **10**（正确 **6**）——上一题 `i<=4` 恰好是 10，他**没算就把上题答案搬过来，而且没交表格**。这不是粗心，是"跳过计算直接凑数"，考场上必死。已登记 **#C002**，并立**新判据：只给数字不给过程 = 一律按错题重做。**

**09-13 上午 · 这三样欠账的清算结果**：
① #C002 `for(i=1;i<=3;i++) s=s+i;` **已销** —— 答 **6** ✅，自己给出三轮过程 `0+1→1+2→3+3`，**没再搬上题答案**。（他说"电脑上不好画表格"→ 已确认：一行一轮的写法就是表格，今后按此交，别再被格式卡住）
② 花括号 `for(i=1;i<=2;i++){ printf("A"); printf("B"); } printf("C");` **已销** —— 答"两行 AB 一行 C"，**内容三件全对**：循环 2 轮 / 括号内 A、B 各执行 2 次 / 括号外 C 只 1 次，正是 09-10 第三课考点。⚠️ **偏差在题面不在他**：本行原写"共 5 行"是错的，gcc 实跑输出 `ABABC` = **5 个字符、0 次换行** —— `printf` 从不自动换行，**只有 `\n` 才换行**。记为教练侧订正，不记学生错。
③ `x→0` 分岔口 **未清**（他要求挪到下次课，继续挂账）：只出一道 $\lim\limits_{x\to 0}\frac{1}{x}$，答 0 就是把 $x\to\infty$ 规则硬套（回炉 1.11 讲义第四节）。

~~**下次开课**：先还 `x→0` 那笔（已挂 4 天），再清今日新撞的 `a++`/`%d`/逗号 三连错。~~ → **09-14 上午更新：`a++` 已在 C 语言话题内销账（见下）；`x→0` 属高数，挪到「继续 高等数学」话题收；`%d` 并入 1.3 新课。标识符已于 09-13 教完并 6/6 过关，此条销。** 工具继续封刀。

**🟢 09-14 上午 · C 语言话题首战：`a++` 真过关（第 3 格）**：三连错里最难那根拔掉，过程不粉饰 —— **首答仍反**（`a=5; b=a++` 答成 b=6/a=5，"延迟加"模型还在），且只丢两个数字没标谁是谁，按硬判据"只给数字不给过程"判**重做、不判死**。当场只教**一条**新规矩「**加号站哪边，哪边先做事**」+ gcc 分行实测，随后 `a=5` 三题组（`b=a++`→5/6、`b=++a`→6/6、单独一行 `a++;`→6 且自己答"单纯a+1"）**全对并交过程**；再用**换名+换序+跨行依赖**的 `int x=7; p=++x; q=x++;` 复检 → 答 **p=8 / q=8 / x=9，理由逐字正确**，并自己识破"上一行已把 x 改过"的陷阱 → **同型连对 2 次，#C003 当场签销**（⚠️ n=2 封顶，约 09-17 再验一次才算稳）。
> 🎯 关键转折：规则**不是塞给他的** —— 上一轮他自己说出"读 `a++` 输出的都是旧数字"，本轮三题全对是**他用自己的规则算出来的**。这是开课以来第一次"生成的知识"，不是"复述的知识"。
> 🛠️ 教练侧自记两笔（**不记学生错**）：① 演示时写了 `printf("%d", a++, a)` —— 同一行内既改又读同一变量属**未定义行为**，输出不可作凭据，已当场划掉并补规矩「**一行动 a 一次**」；② 收尾时把逗号题 `(a++,a++,a++)` 的答案**直接说破了** → 该题判据作废，下次**必须换同型题**重考。
> 📍 下一步：按用户自报进度（1.1/1.2 已学完）开 **1.3 顺序程序设计**，第一课只讲 `printf` 的**坑位数**，顺手把 09-13 欠的 `%d` 那笔并进来清。

**🟢 09-14 晚间 · 1.3「顺序程序设计」整节收官**（同日上午开讲，分五节讲完 + 两轮共 8 题实测）：
**讲课内容**：一 printf 的坑位数 · 二 逗号的两副面孔 · 三 `%5d` 排队站位 · 四 scanf 与 `&` 的方向 · 五 回车为什么会吃掉你的输入（吃换行符）。
**判分**：首轮 8 题 **81%（数字 5/5 全对）**，但**理由 3 处不实**（整数除法答成"打印时砍掉"；`&` 答"因为这是 scanf 语句"= 复述题目；把 `&a` 说成"把 a 送到 scanf 手里"）；第 ⑤ 题 `%5d` 未答。重讲后交 `a=5 / ch=回车` ✅，终两题 `printf("%d",'\n')`→**10** ✅、`printf("%c",65)`→**A** ✅。
**✅ 当场销账**：#C004 `%d` 坑位数（首考 1/3 → 同日重做 **3/3**）。
**🔴 遗留 3 笔欠账（下次开课首检，顺序不可换）**：① **scanf 吃换行符**——必须**换同型题**（原题答案已在讲义里）；② **`%5d` 逐字符默写**——标准答案 `<····7>` = `<` + 4 空格 + `7` + `>`（**7 个字符**），他没默过；③ **`&` 的方向性**——口述，**禁止答"因为这是 scanf 语句"**。
**🔴🔴 本场最大发现：判据有洞。** 8 题 81%、数字全对，他仍说「**这一部分还是看不下去**」→ **正确率抓不住"听不懂"**。第五节首版失败根因查清：一次塞了 6 层（两张票 + `%c` 不挑 + 清洁工空格 + `getchar` 扫把 + 三样输入 + `'\n'` 写法）；推倒重讲、**只留「点餐台排队票」一个画面**后他随即答对。
**⚙️ 节奏改判（用户拍板）**：一节一课保留，但**每节讲完先问「有没有哪句话看不懂」，他答了才进下一节**；正确率之外**加这道人工检查**，不再单靠正确率判过关。每讲一个概念**当场报它在考卷的分量**（他问"这些全要背吗"逼出了下面的权重实查）。
**📊 考点权重实查（不让他白背）**：八份真题 grep `ASCII` / `大小写` / `字符的编码` → **命中 0 条**；`getchar` 十年仅 1 次（2022 年，答案 D）→ 编号表**不考背诵**，只给 5 项最低清单（`'\n'`=10、`'\t'`=9、空格=32、`'A'`=65、`'a'`=97，记"大小写永远差 32"一条就够）。
**🛠️ 教练侧自记（不记学生错）**：① 表格用中文【】框输出 → 他被"方括号"绕住，已改为「程序印的是英文 `[`，中文括号只是我用来框住空格」；② 演示写了未定义行为 `printf("%d", a++, a)`（上午已记）；③ 收尾把 `(a++,a++,a++)` 答案说破 → 判据作废；④ **`%5d` 空格数我数错**：旧文写 `<···7>`（3 空格），gcc 实跑 `<    7>` = **4 空格**；⑤ **备题答案我曾凭印象编错** —— 旧写 `x=(a+5,a*3)` "答 x=6,a=6"，gcc 实测 **`x=3 / a=1`**（`a+5` 白算、a 没变）。→ 下次逗号备题改用 **`int a=1; int x=(a++, a*3);` → gcc 实测 `x=6 / a=2`**（同时复检 #C003）。**规矩重申：写进看板的"标准答案"必须有 gcc 凭据，禁止凭印象。**
**📄 沉淀产物**：新建讲义 `docs/posts/computer/notes/1.3b-格式化输入输出-零基础.md`（已登记侧栏 `config.mts` + `notes/index.md`；**全部代码 gcc 实测，未复制课本**）；错题本新增 **#C004 / #C005**；开场卡 `C语言.md` 欠账与进度同步。→ **下次「继续 C 语言」清完三笔欠账后进 1.4 选择结构程序设计（`if`）**。

**🎓 09-13 教学事故 → 模式已固化成技能 `skill:step-teacher`**（工作区级 `.reasonix/skills/step-teacher/SKILL.md`，已确认索引到 status:ready）。事故本身记清楚：本节后半段我一次灌了 **9 个考点**、他零练习纯听 → 随即**三道题全错且同根**（把 `a++` 听成"延迟加"）。技能五条红线：一轮 1 个概念 / 符号先翻译成人话 / 能跑就 gcc 实测 / 讲完立刻练 ≤3 题 / 判卷只认过程 + 有脚手架答对不算掌握 + 教练自己错了当场改账。
**新话题怎么用**：粘贴任意章节 + 说「跟着我的笔记站一步一步教学下去」即触发，高数同样适用。**高数没有编译器 → 取证改用 node 数值表 / 特殊值回代（让他自己跑、自己看趋势）**；欠账题 ③ 的答案**故意不写进看板**，必须他独立算出来才算销账。

**🧾 2026-09-13 全天结算（09-14 凌晨 02:35 补记，数字全部来自 `学习日志.jsonl` 实查，不靠印象）**：
当日 **10 道题**：过关 6 · 部分 1 · 全错 3 —— **是 09-07 开课以来做题最多的一天**，先记功再挑刺。
新增过关格：#C002 `for(i<=3)` 销账（答 6 + 自交过程 `0+1→1+2→3+3`）、标识符合法性 6/6（首考 0/5 当天翻盘）、花括号作用域 3/3、变量命名 `grade`、字节↔位与单位阶梯 2/2、「256 种为何最大 255」1/1。
当日新撞墙（**下次课必须先还，别开新坑**）：`a++` 落地时机 / `%d` 坑位数 / 逗号表达式取末值 **三连错 0/3**，病根唯一 —— 把 `a++` 当成「延迟加」，实际是「交出旧值 + 立刻改盒子」同时发生。
🛠️ 工具侧（**一律不计学习量**）：背词本 `D:/word-master` 加了「全部 2230 词里的第 N 个」显示，改 `app.js`/`style.css`/`index.html` 三文件，只读 `WORD_INDEX`，**`data.js` 词库一字未动**（`git diff` 可验）。封刀戒律第 **3** 次被破，如实记账。

**📌 分科开话题协议（09-14 起，用户要求「不同学科在不同话题里完成每天的学习」）**：
本看板是**唯一事实源** —— 话题开多少个，进度都只写回这里，绝不另存一份。
四个学科各配一份开场文件：`备考计划/开新话题/C语言.md` · `高等数学.md` · `计算机基础.md` · `英语.md`
**用法**：新话题第一句只说「继续 <学科>」，AI 必须先读本看板 + 对应开场文件，再从「待验证区」接着往下教，**禁止从头问起、禁止重新自我介绍**。
> ⚠️ **落实边界（已实查 Reasonix 能力清单，不承诺做不到的事）**：本机**没有「创建会话 / 置顶会话」的接口**，新话题与置顶只能由用户手动完成；AI 能交付并已交付的是这四份开场文件 + 本协议 + 看板交接协议。

**⚠️ 封刀戒律本轮已破（09-10 凌晨，诚实记录）**：又建了门户「🪞 复盘」+「📐 公式台」两个标签页、接了 `学习日志.jsonl` 接口、顺手修了 favicon 404。**这些是弹药，不是学习**。三件事已做完（日志沉淀 / 公式可视化 / 英语正确率叫停提醒），**即刻再封刀，下次会话不开任何工具类任务**。真进度只有科目进度条那 8%，下面这些页面一行都不算。

**📇 Anki 英语高频词快照（09-12 17:30 实测，直读 `collection.anki2` 的 revlog 操作日志，不认自报）**

| 项目 | 数值 | 人话 |
|:---|:---|:---|
| 词库总量 | 2230 张 | 已开课 991（**44.4%**）· 从未碰过 1239 |
| 今日到期 | 180 张 | 测量时**今日做题 0 次** —— 一张都还没动 |
| 逾期最久 | **22 天** | 最早一批到期卡拖了三周，它不会自己消失 |
| 记得牢（间隔≥15天） | 334 张 | 其中间隔≥30天的只有 **138 张** |
| 忘过至少一次 | 59 张 | |
| 全历史做题 | 3734 次 / **3.8 小时** | 平均 3.7 秒一次 —— 手很快，多半在"认脸" |
| 全历史正确率 | 61.2% | 近 7 天 2874 次、**54.2%**（在往下掉） |
| 按钮分布 | 困难 **33.2%** · 重来 5.7% · 保留率 94.3% | 见下方判读 |

**判读两条，都可核对**：
1. **困难按到 33.2%、重来只有 5.7% —— 你几乎没有"真忘掉"的词，是自己把词按成"难"的。** 每点一次困难，Anki 就把这张卡的聪明值永久砍一刀，砍完它就被排得更频繁。这是**亲手制造假难词**，不是记忆力差。改法一句话：**想起来了就点「好的」，「困难」只留给"看了答案才想起来"的那种。**（保留率 94.3% 说明排期本身没坏，问题在手不在算法。）
2. **开课率 40%（09-10）→ 44.4%（09-12），两天涨约 100 张新词**（09-07 一天就开了 788 张）。全历史**新学 : 复习 = 1.8 : 1**，而零基础建词库这个比例**应该反过来**。新词开太快，复习债就滚成今天这 180 张到期 + 22 天逾期。
   **药方**：先按每天 20-30 张清 22 天的欠账（约 41-62 天追上），期间**每天新词不超过 20 个**；做完就收手，别为清库硬刷。

**📇 Anki 英语高频词快照（09-22 01:20 实测，直读 `collection.anki2` 快照，含 `-wal` / `-shm`）**

| 项目 | 数值 | 人话 |
|:---|:---|:---|
| 词库总量 | 2230 张 | 已开课 1196（**53.6%**）· 从未碰过 **1034** |
| **今日真实到期** | **465 张** | 复习 457 + 学习中 8 |
| 🔴 **软件显示的「到期」** | **200** | **这是「复习每日上限」，不是到期量** |
| 🔴 **每日欠账** | **265 张** | 465 − 200，顺延到明天，明天再叠新的 |
| 今日已做 | **402 次** | Again 12 · Hard **232（57.7%）** · Good 127 · Easy 31 |
| 全库按钮（6183 次） | Hard **41.9%** · Good 29.3% · Easy 20.1% · Again 8.8% | 对比 09-12 的 33.2% → **恶化 8.7pp** |
| 新卡每日上限 | 40/天 | 光过完 1034 张新卡要 **26 天** |
| 卡片总数（全牌组） | 2292 张 / 复习记录 6183 条 | 8 个牌组 |

**判读三条，都可核对**：
1. 🔴🔴 **「200 张到期」是软件上限，不是真实到期量。** 学生自陈「今天有 200 张到期」→ 直读 `deck_config` 发现字段 10（复习每日上限）= **200**，而真实到期 **465** → **每天被拦下 265 张**。**这是「怎么都做不完」的机制性原因，不是学生不努力。** 改法：牌组 ⚙ → 选项 → 每日上限 → 最大复习数 `200 → 400`。
2. 🔴 **Hard 从 33.2%（09-12）涨到 41.9%（09-22），今天更是 57.7%** —— 09-12 那份快照的药方**没执行**。这正是本项目要盯的第二类漏洞：**方案存在但不落地**。
3. **新卡 1034 张（08-11 导入）从未开课** + 开课率 44.4% → 53.6%。**先把 465 清干净、新卡上限压到 15**，再谈加新词。

---

**📌 09-22 补充：可行性评估结论（数据见 `可行性评估与提效建议-2026-09-22.md`）**

| 项 | 值 |
|:--|:--|
| 推进日密度（**修正后**） | **50%**（8/16 天）—— 原记「23%（3/13）」是**低估** |
| 出勤率 | **75%**（12/16 天有记录） |
| 所需推进日 | 29 模块 ÷ 0.667 = 43.5 个 → ÷ 50% = **87 天** |
| 倒计时 | **160 天** → **富余 73 天** |
| 🎯 **临界速率** | **0.3625 模块/推进日**（实测 0.667，是其 1.84 倍） |
| 🔴 单科断档 | **计算机基础断 9 天**（09-13 至今）—— 属 200 分专业综合课（占 40%） |

> 📌 **口径澄清（2026-09-22 夜）**：日志里 `subject` 字段写作「**计算机**」的那 4 条（09-13 的字节↔位 / 单位换算阶梯 / 256 与 255 / 硬件五大件），
> 对应的是 **计算机基础**（= 四个学科之一，见上文 `计算机基础.md`），**不是 C 语言程序设计**。
> 二者同属「计算机基础与程序设计」200 分那门课，但**是两条独立的教学线**（理论 vs 语法），排期分开算。
> ⚠️ 此前本表写「计算机断 9 天」易被读成 C 语言 —— 已改为全称。

---

## 📌 当前阶段（打地基：2026-09 上旬 - 11 月底）

| 事项 | 状态 |
|:---|:---|
| 主线目标 | C 语言推到数组/函数/指针；高数推到导数与求导法则 |
| 每日配额 | 4-5h（高数 2h + C 语言 2-3h 交错）+ Anki 英语词 15min |
| 科学规则 | 提取练习优先（先做题再讲解）、交错出题、错题进 Anki `专升本::错题集` |
| ⚠️ 风险 | 截至本次会话**还没做过任何一道题**——警惕"囤资料/完善工具"式伪勤奋 |

## ✅ 上次会话完成的事（2026-09-09 下午～晚间）

### 学习事件（本会话首次出现，20 分钟微课 + 6 题实测）

用户明确否决"继续做工具"（"这些知识我还一个都没学，怎么写"），会话转入教学。改上 20 分钟微课（5 样东西：lim 读法 / x→0 与 x→∞ / ∞ 不是数 / C 最小框架 / 整数除法），随后实测：

| 项目 | 得分 | 判定 |
|:---|:---:|:---|
| C 语言最小框架默写 | **20/20** | ✅ 掌握（09-07 是 0/20，框架残缺已消除） |
| 高数极限 4 题（读+算） | **20/40** | ⚠️ 未掌握，缺口极明确：**会读符号、不会算结果** |

- 题 2 只把 `lim(x→∞) 1/x` 翻译成人话就收笔，没写出"= 0" → 按 DeepTutor 的 `error_diagnosis` 记**应用错误型**
- 题 4 判断正确但不说原因 → 按 `mastery.py` 的 `_CONFIDENCE_CAP`（confidence=2 封顶 0.8）只记 0.5，**不算掌握**
- 书写细节：把「无限」写成「无线」；`printf("wpc ")` 引号内多一个空格 → 已立规矩：输出要**逐字符自检**（考场没有编译器替他报错）
- 原 09-09 那 15 题计划**全部作废不做**（对当时的他量级不对），已改为此 20 分钟微课
- ~~**欠交 3 题**~~ → **09-09 深夜已全部交回，见下方第二轮实测**

### 第二轮实测（09-09 深夜 → 09-10 凌晨，同一会话续测）

| 项目 | 得分 | 判定 |
|:---|:---:|:---|
| 高数极限 6 题（含欠交 3 题 + 抓大头 2 题 + 判断说理 1 题） | **100/100** | ✅ 上一轮的「会读符号不会算结果」缺口**已闭合**（同考点 50% → 100%） |
| C 语言 for 循环人肉编译 | 高数满分同时 **0/40** | ❌ 撞新墙，且根因在教练（见下） |

- 题 ① `lim(x→∞) 5/x = 0` ✅ ② `lim(x→∞) (1/x+3) = 3` ✅ —— **这次写出了等号和一个数**，正是上一轮缺的那一步
- 题 ③ 判断"错"+ 理由「2x 无限趋近于无穷大怎么会等于 0」✅ —— 用自己的反问句说出来的，不是背术语，判 passed
- 题 ④⑤（抓大头同型 `\frac{2x+1}{x}`=2、`\frac{5x-3}{x+2}`=5）✅ 秒答，配角项 `+1`/`-3`/`+2` 正确无视
- ❌ **⑥ for 循环答 4（正确 10）**：把计数器 `i` 的末值当累计器 `s`，且以为循环只跑 1 轮 → 典型**未教先考**。他当场反馈「你是不是忘了我都是零基础」，**成立**
- ✅ **A 填表（有脚手架）全对 1→3→6→10**，❌ **B 独立做 `a=a+2; a=a*2;` 答 6（正确 10）** → **假掌握警报**：撤掉脚手架就错，说明"会"只存在于表格里
- ⚠️ ③C 题（`i<=3` 版）**未交**；④花括号范围题**未交** → 进待验证区
- 📌 教学纪律新增：**一次只教一个概念；没教过的东西不出题**（本会话唯一失误归教练，不归学生）

### 工具侧产出（弹药，不是学习；本轮已封刀）

工具侧产出（弹药，不是学习）：
- 门户 `D:\studio`：KaTeX 本地化（`vendor/katex`，1.5M，离线可用）+ 公式已实测渲染成真数学；`/api/drill` 改用 `--total`（原来误用 `--count 5` → 9 源出 45 题且绕过随机分布）；新增 `POST /api/drill/reshuffle`「换一批题」
- 出题脚本 `scripts/daily-drill.mjs`：`distribute()` 修掉补齐偏置 + 加 `BANK_WEIGHT` 加权（高数/C编程权重 3，政治/简答 1，落实"数计主战场、英语降权"）；**答案改为默认折叠**（旧 `--closed` 语义反了，重命名为 `--open`）
- 读题教练：新增「n 项和的极限 · 夹逼准则」一类（上下两道墙三步套路 + 两大失效信号 + 不得分写法），对应 09-09 那道真题
- ~~`plan/每日刷题-2026-09-09.md`：15 题手写版~~ → **晚间全部作废不做**（对当时零基础的量级不对），已改为 20 分钟微课 + 6 题实测
- 晚间补齐：`anki.mjs` **已接进 `GET /api/anki`**（CJS 动态 import + 60s 缓存 + in-flight 锁）；门户新增「今日三步」与「Anki 卡片进度」两面板，英语牌组标为**保鲜轨**并视觉降权
- 两轮子 agent 交叉审查共修 10 处，含我自己引入的 4 个新缺陷：[blocker]「换一批」点了完全无响应（裸 `data-reshuffle` 是空串 falsy）、[high] 今日三步勾选跨日不清零会谎报"已交 3/3"、[high] 我新加的长度签名会碰撞致陈旧渲染、[medium] 并发请求互踩临时 sqlite；顺手删掉 Google Fonts 外链（原本违反离线约束）
- ✅ **门户已重启（09-10 凌晨，此条销账）**：`taskkill` 旧 PID + `nohup node server.js` 拉起，实测 `/`、`/api/anki`、`/api/log`、`/math/`、`/favicon.ico`、`/studio.css` **全部 200**。并用 Playwright 真机点开「🪞 复盘」「📐 公式台」验证渲染：复盘页 4 个区块数字全对、公式台 46 处 KaTeX 全渲染 0 残留。控制台唯一那条 error 是 `/favicon.ico` 404 → 已复用 `sakura.ico` 修掉（顺手给标签页上了樱花图标）
- 🆕 **新增「🪞 复盘」标签页**：数据源是 `备考计划/学习日志.jsonl`（逐行 JSON，服务端 `readStudyLog()` 解析，坏行跳过不报错），走 `GET /api/log`。四块：概览统计 / **已过关**（只认 `mastery=passed`）/ **待攻克**（`failed` + weak 标签云）/ 时间线。**判据写死在页面上**：撤掉讲义和表格后独立做对才算过关，有脚手架做对记「假掌握」不算进度。实测渲染：14 条记录 / 3 过关 / 4 待攻克 / 覆盖 4 天
- 🆕 **新增「📐 公式台」标签页**：同源挂载 `D:/math-notes`（`MOUNTS` 加 `/math` 前缀），把上一步做的离线公式页嵌进门户，`serveStatic` 对 `/` 结尾路径自动补 `index.html`。**为什么挂门户而不是走笔记站构建**：vitepress build 被门拦 + 要重启门户，而这份 HTML 自包含、离线、不需构建
- 🆕 `studio.css` 是**纯暗色主题**（`:root` 里 `--bg:#1a1620`），新页面配色要按暗色写，别照抄笔记站的浅色
- ⚠️ **本仓无 git 保护**（`D:/studio` 不是 git 仓库，`git fetch` 实跑失败）→ 改坏了没法回滚，每次动 `server.js/studio.js` 前后必跑 `node --check`

### 🔨 工具侧产出第二轮（09-12 ~ 09-13 凌晨）——**封刀戒律又破了，诚实记一笔**

**破刀事实**：本轮用户明确要求改门户（首屏砍到三块 / 背词页只留配额+背词本 / 家教要真能启动 / 掌握度跟踪），我照做了。**这些仍然是弹药，不是学习**。真进度只有下面「科目进度条」那 8%，掌握度清单上那 8 个考点**一个都没过关**。

- **新增 `D:/studio/focus.js` + `focus.css`**（首屏三块：👉下一步 / 📊掌握度清单 / 🍅计时）。算法**照抄 DeepTutor 源码**不自己发明，出处都标在常量注释里：近因权重 `RECENT_W=[0.5,0.7,0.85,0.95,1.0]`（`learning/mastery.py:17`）、置信度封顶 `CONF_CAP={1:0.5,2:0.8}`（`mastery.py:21`，专治"做对一题就说会了"）、过关门限 `GATE=0.9`（`policy.py:35`，Alpha School "90% before you advance"）、复习间隔 `[0,1,3,7,14,30,60]` 天（`scheduler.py:14` 的 MEMORY 序列）。**状态是派生值**：只存作答记录，每次现算，不存"已掌握"字段。
- **老面板一个没删**，只整体搬进折叠 `<details class="plan-more">`，id 全部保留 → `studio.js` 的 refresh 照常命中。回滚 = 删 `studio.html` 两行。
- **`server.js` 新增 `GET /api/mastery` + `POST /api/mastery/attempt`**（手动 ✓/✗ 落 `data/mastery-attempts.json`，按考点分桶存最近 20 条）。
- **算法单测 36 项全绿**（`node` 跑真实 `学习日志.jsonl`）。过程中抓出并改掉**我自己编的规则**：复习档位原先写成"连续答对次数=档位"，改回逐条重放 `schedule_next` 状态机；日期格式化改本地时区（UTC+8 下 `toISOString` 整体差一天）。
- 🎯 **实测数据（09-13 00:18 首屏截图）**：有记录考点 **8 个 · 过关 0 个**；下一步自动指向「先清 Anki 到期：还剩 166 张」（到期 173 − 今日已做 7 = 166，两个数字对得上）。手动点 ✓ 已验证真能改数字：`C-for循环` 掌握度 0%→51%、做过 1→2 次（封顶 50%→80%），toast 正常、服务端落盘。

### 🚨 本轮最该记住的一条教训（与工具无关，是方法论）

**`focus.js` 写完后 `node --check` 通过、36 项单测全绿 —— 但它在浏览器里一次都没执行过。**
根因：`server.js` 的静态路由是**逐个登记白名单**，我只加了 `studio.html` 的 `<script>` 引用，忘了登记 `/focus.js`、`/focus.css`，结果 **404 静默失效**。连带查出 `desk.css`、`word.css` 早就在 404（页面只是"看着样式有点旧"，一直没人发现）。
→ **教训：语法检查和单测都证明不了"代码被加载了"。前端改动必须开浏览器看 console。** 今后新增根目录 css/js，`studio.html` 和 `server.js:ROOT_ASSETS` 两处都要改。
→ 同类风险：门户里任何"看起来在跑"的东西，先问一句"我亲眼在浏览器里见过它吗？"

### ✅ 家教（DeepTutor）从"点了没反应"到真能用

**推翻了长期结论「DeepTutor 冷启动很慢」** —— 是**假慢**。真因是启动器 `D:/studio/launch-deeptutor.cmd` 从来没把服务拉起来过，三层 bug 逐层剥：
1. 文件存成 **LF-only** → `cmd.exe` 错行解析，把中文注释切成 `launch`/`cal`/`1` 当命令跑，报"系统找不到指定的路径"却 **EXIT=0 假成功**（所以看起来"点了没反应"）；
2. 中文注释 + 无 `chcp` → cmd 按 GBK 解码，某些字节对吞掉行尾 CR 把下一行粘成碎命令；
3. **决定性一层**：`start "标题" /min cmd /c ...` 需要控制台，而门户是 `node spawn({stdio:'ignore'})` 无控制台调用 → 建不出窗口、子进程静默死亡。改用 `powershell Start-Process` 解决。
**实测**：点门户按钮 → **73 秒**就绪，3782/8001/9999 全 200，`/api/status` 里 `deeptutor online=true`。embedding 加载 bge 模型只用约 15 秒。
遗留（不影响可用）：启动器里 embedding 用裸 `python`（解析到系统 3.13.15 而非 `.venv`），日后若依赖 venv 包需换绝对路径。

### 📇 Anki 复测快照（09-13 00:18，门户直读 revlog）

今日到期 **173 张** · 今日已做 **7 张** · 逾期最久 **22 天** · 开课率 **44%**（已学 991 / 未学 1239）· 每日目标 50 张 → 还剩 43 张。
**与 09-12 17:30 那次对比：到期 180 → 173，说明中间只刷了个位数张。药方（每天 20-30 张清欠账、新词≤20）继续有效，未变。**

### 🖥️ 本机首个 C 编译器已装好（09-10 凌晨，用户拍板「本机装 w64devkit」）

**这条推翻了长期事实「本机无任何 C 编译器」** —— 今后出题可要求"写完自己跑一遍验证"，不再只能靠在线编辑器。

- **`skeeto/w64devkit` v2.9.1**（GitHub 4772★；实查 `get_latest_release` 确认 2026-08-10 发布、自带 **GCC 16.2**、61MB 免安装自解压。选型来自本轮 GitHub 实查，非凭记忆）
- **完整性双重校验通过**：下载 61462208 字节与官方一致；`certutil` SHA256 = `9208c19755cd4964b7915b9afcf02c66d493a4c870c4b3e83f6c538d9c1237a5`，与 GitHub asset `digest` **逐字符一致**
- 路径 `D:\w64devkit\w64devkit\bin\gcc.exe`（**套了两层 w64devkit**；PATH 必须给到 `bin`，否则 gcc 找不到同目录 `as.exe` → `CreateProcess: No such file or directory`）。占用 594M
- **一键入口 `D:\c-practice\run-c.bat`**：把 `.c` 文件**拖到图标上**即编译+运行；内容纯 ASCII（cmd 是 GBK，中文注释会乱码）。双路径已实跑验证：
  - ✅ 成功：`练习1.c` 输出 `A B A B C`（中文文件名正常）
  - ✅ 失败：`练习2-有错.c` 故意漏 2 个分号 → 精确报 `4:5: error: expected ',' or ';' before 'for'`，**带行号带波浪线**
- ✅ **SENDTO 已补上（09-10 凌晨，此条销账）**：`%APPDATA%\Microsoft\Windows\SENDTO\CC-compile-run.bat` 已写入并 `ls` 确认存在 → 现在**右键任意 `.c` 文件 → 发送到 → CC-compile-run** 即可编译+运行。桌面 .lnk 仍建不了（本机 PowerShell 是 ConstrainedLanguage 受限模式，`WScript.CreateObject` 被禁），拖文件到 `run-c.bat` 那条路照旧可用
- 🎯 **装它的教学理由**：考场没有在线编译器替他纠错。gcc 是"可控的报错环境"，用来练**读错误信息 + 逐字符自检**，不是用来依赖自动补全

### 讲义沉淀（应用户要求「整理文档沉淀下来」）

- 新建 **`docs/posts/computer/notes/1.3a-变量与赋值-零基础.md`**（263 行）—— 变量=盒子 / `=` 是从右往左"塞"不是相等 / for 的花括号管到哪一行 + `}` 是回程车票 / 完整人肉编译表格 / 自检 5 题+易错对照表+答案。直接对应本轮 ⑥ 和 B 题的两个错点
- 新建 **`docs/posts/math/notes/1.11-极限求值零基础-抓大头.md`**（179 行）—— **用 `$$` 真数学公式**（站点 `markdown.math:true` 已开）：lim 读法 / x→∞ 两种下场 / 抓大头 / 三条速查结论 / **⚠️ `x→∞` 与 `x→0` 分岔口（同一式子答案相反）** / 三步流程 / 自检 6 题+答案
- 已挂进侧边栏 `config.mts`（两处）+ 两个 `index.md`（含篇数 12→13）
- ✅ 死链检查 `node scripts/check-links.mjs`：**新增 4 条链接全部有效**；仅存的 7 处死链是历史遗留（政治 2025 的 `/resources/政治选择题题库` ×2、日语与 knowledge 的 `[[toc]]` ×5），与本次无关
- 错题本 `备考计划/错题本模板.md` 已填 **#C001**（B 题答 6 那题），`node scripts/mistake-stats.mjs` 实跑解析正常（错题 6→7）
- ✅ **讲义已上线（09-10 下午，此条销账）**：`npm run docs:build` 跑通（96.9s，上一轮的 2 条 `/resources/政治选择题题库` 死链已改成文字标注才过构建 —— 注意**死链会直接 fail build**，不是只警告）。实测门户 URL 全 200：
  - `http://127.0.0.1:43210/darling016123/posts/computer/notes/1.3a-变量与赋值-零基础.html`
  - `http://127.0.0.1:43210/darling016123/posts/math/notes/1.11-极限求值零基础-抓大头.html`
  - 📌 **门户前缀是 `/darling016123`（不是 `/notes`）**，`server.js` 的 `MOUNTS` 按 dist 的 base 挂载；构建完**不需重启门户**（静态文件直读 dist）
- 🆕 **应用户「主要是数学的公式」→ 建了离线自包含公式页 `D:\math-notes\高数极限-公式与人话对照.html`**（双击即开：不需联网、不需开门户、不需构建）：同目录带一份 `katex/`（与门户那份 md5 一致 `3ca9db95…`）；页面内引用已全部改为**相对路径**（原来的绝对 `file:///` 引用一旦挂到 http 就会被浏览器拒绝加载脚本，属真隐患，已修）。内容 = 樱花风 + **每条公式配一句人话** + 答案折叠 + ⑬a/⑬b/⑪ 实做。**验证：用页面自带引擎逐条 renderToString，46 处公式 0 失败**
- 📌 ~~教训（写给教练自己）：**聊天窗口不渲染 LaTeX**，一律用 Unicode 人话排版~~ → **🔧 2026-09-19 订正：这条旧规矩已废弃。** 客户端**现在能渲染 LaTeX**，用户明确要求数学题**按真数学排版显示**（`lim` 带下标 + 分数横线），**且不要用代码块包数学**（会显示成灰底等宽纯文本，看不清分数和下标）。今后出题：块级 `$$\lim_{x \to 2}\frac{1}{x-2}$$`、行内 `$\lim_{x\to0}$`；**代码块只留给命令**（如 `node -e "..."`）。

### 历史（2026-09-07 凌晨～04:10）

- 知识库完善冲刺已推送（b8097c0）：新增 5 个必杀专项（3.4 循环数组 15 题 / 3.5 指针 / 3.6 递归 / 2.4a KMP / 2.5a 二叉树建树）、高数 2025 真题 OCR 文字化、1.10 极限三法决策树
- 桌面资料核查：`桌面/广东专升本资料/`（2.1GB）已基本文字化入 `资料/`（93 篇 md）；孤本排期待 11 月二轮（政治历年 doc、英语完形 100 篇、3400 词表、投档数据）
- "2000 必刷题"确认**未采购**，裁决：属第二轮（11 月）弹药，暂缓
- **Anki 闭环首跑完成（160e112）**：C01 基础语法 22 张 + 极限三法 12 张已真实入 Anki（牌组 专升本::C语言/专升本::高数，笔记类型"问答题"）；TSV 产物在 `knowledge/anki_cards/`
- **5 道试水题已作答并批改（≈25/100，真实基线）**：
  - 题1 for循环 0/20（只写出 `#include <stdio.h>`，自述"压根没写过"）｜题2 7/2 10/20（=3 ✅；得 3.5 的改法错——直觉摸到 float 方向）｜题3 (x²)′=2x 10/20（答案对但没写幂函数求导依据）｜题4 lim sinx/x 0/20（**不认识 lim 符号**——符号扫盲是头号缺口）｜题5 未作答
  - **6 张错题卡已入错题牌组**（专升本-C语言错题 3 张 / 专升本-高数错题 3 张，tag:试水题批改）——闭环第二环已实跑

## 📝 本周任务（2026-09-07 ~ 09-13）

- [x] ~~完成 5 道试水题并提交答案~~（09-07 已作答+批改+错题入库 ✅）
- [x] ~~交清欠的 3 道补测题~~（09-09 深夜已全交且全对：① =0 ✅ ② =3 ✅ ③ 判断+理由 ✅ → 高数 6 题 **100/100**）
- [x] ~~交 ③ C 题 + ④ 花括号范围题~~（**09-13 两笔均已销**：`i<=3` 答 6 + 自交过程 `0+1→1+2→3+3`；花括号内容三件全对。题面"共 5 行"是**教练错**，gcc 实证 `ABABC` = 5 字符 0 换行）
- [x] ~~`a++` 落地时机（09-13 三连错之一）~~（**09-14 销账**：同型连对 2 次，#C003 签销，⚠️ 约 09-17 复检）
- [x] ~~🔴 **C 语言剩两笔欠账**~~ → **09-14 晚间清完**：`%d` 坑位数同日重做 **3/3** ✅（#C004 已登记，⚠️ n=2 约 09-16 复检）；逗号表达式取末值 —— 无变量版 `int x=(5,8,2)` 已答对 ✅，**带变量版仍欠**（⚠️ 原题 `(a++,a++,a++)` 答案已被教练说破泄露，禁止再用；备题 `int a=1; x=(a+5,a*3);`，改随 1.4 新课练习穿插，**不当开课门槛**）
- [ ] 🔴 **高数欠账**：$x\to0$ 分岔口（已挂 4 天）→ **在「继续 高等数学」话题收，不在 C 语言话题重复考**
- [x] ~~B 题无脚手架重做~~（`a=a+2; a=a*2;` 09-09 答 6 → **09-10 凌晨合上讲义重做答 10 ✅**，同型新题答 14 ✅，#C001 已签，09-12 前再抽查一次防遗忘）
- [x] ~~lim 符号扫盲~~（09-09 晚间已过关：能读 lim/x→∞ 并翻译成人话 ✅）
- [x] ~~第三课·变量与赋值~~（09-10 凌晨已插队教完：变量=盒子 / `=` 是塞不是相等 / 花括号管到哪行 → 讲义已沉淀为 **1.3a**）
- [x] ~~第三课续：`scanf` 让程序"会听话"~~（**09-14 已教**：scanf 与 `&`（课本 p98/p99）、getchar/putchar、吃换行符。⚠️ 他自报第五节**「不是很通透」**、题 ⑧ 主动留白 → **下次课第一题就是 ⑧**）
- [x] ~~🔴 **C 语言下次课（09-15）四笔欠账，按序收**~~ → **09-14 晚间重排**：①②③ 三笔仍是下次「继续 C 语言」的**开课首检门槛**（① scanf 吃换行符换同型、② `%5d` 逐字符默写、③ `&` 的方向性，禁答"因为这是 scanf 语句"）；④ 整数除法理由 —— **当晚已建立模型**（`printf("%d",7/2)`→3 ✅、`int i=1/2`→**0** ✅，此题**根本没有 printf**，正好自证刀在除法），**不再占首检**，改为约 09-16 随口复查"刀在哪一步"；⑤ 逗号带变量版（原题已泄露）→ **随 1.4 新课练习穿插**，不当开课门槛
- [ ] **本周一句话戒律**：不再改任何工具/脚本/门户。工具再不好用，也只记在「待办-工具」里，等本周日后再动（本轮已破戒，弹药到位即刻封刀）
- [ ] 开始清 Anki：40 张卡（C语言 22 + 高数 12 + 错题 6）首轮复习
- [ ] 建立 4-5h/天节奏（高数/C 交错，各 2h+）
- [ ] 本周只推 2 个考点：C01 基础语法、高数极限计算（配合新笔记 [1.10 极限三法决策树](/posts/math/notes/1.10-极限计算三法决策树)）
- [ ] 周末做交错小测 8 题 + 清一次 Anki 队列

## 🔍 待验证区（下次会话优先检查）

| 疑问 | 验证方式 |
|:---|:---|
| 🔴🔴 **09-13 学习量终于不再是 0：当日 10 题（过 6 / 部分 1 / 错 3）** | 09-12~09-13 凌晨那场确实纯工具（学习量 0），但 **09-13 上午补了课**：三条旧欠账已销两笔（#C002、④ 花括号，见下方 ✅），只剩 $x\to0$ 在挂。**09-14 起学习按学科拆进四个话题，但进度只写回本看板**（协议见上「📌 分科开话题协议」）。⚠️ Anki 依旧没跟上：09-13 到期 173 张仅做 7 张 |
| ✅ ~~09-13 同根三错~~ → **只剩两样** | **① `a++` 落地时机 09-14 已销账**：首答仍反（`b=6 a=5`）→ 重教「加号站哪边，哪边先做事」→ `a=5` 三题组全对且交过程 → 再同型换名换序 `x=7; p=++x; q=x++;` **全对 + 理由逐字对**（p=8 / q=8 / x=9），并自己复述出第三条规则「单独一行 `a++` 就是单纯 a+1」。⚠️ **n=2 封顶，约 09-17 复检**。错题本 #C003 已登记并签销。<br>**② `%d` 坑位数 09-14 开课未清 / ③ 逗号取末值 仍欠** → 1.3 第一课实测 1/3：① `printf("%d %d",3+4,7)` 答"1 坑 2 客人"❌（正确 2 坑，输出 `7 7`）；② 答"9 个"⚠️ 漏印原样输出的"结果是"；③ `printf("%d",7/2)` 答 3 ✅ **但理由错**（说成"%d 不显示 .5"，实际 `7/2` 在除法那一刻**就已经是 3**，gcc 对照：`7/2.0` 用 `%f` → 3.500000）。<br>🎯 **今日最有价值的一条**：他主动追问「为什么 `printf("%d",3+4,99)` 只印 7 不印 99，我记得有一个是看最右边的输出」→ **他把「逗号表达式取最右」错搬到了 printf 的参数分隔符上**。这可能是 09-13 逗号题答错的**真根因**（他不知道 printf 里的逗号 ≠ `(a,b,c)` 里的逗号）。已当场开讲「逗号的两副面孔」。<br>**→ 同日已重做清账 ✅ 3/3**（`printf("%d%d",1,2)` 答 2坑/2客人/印 `12`；`printf("我%d今年%d岁",3,20)` 连字写全 `我3今年20岁` + 2 坑；`int x=(5,8,2)` 答 **x=2 且自己说出"这里的逗号是取最右"**）。已登记 **#C004**（⚠️ 同日连做 n=2，不算稳）。<br>**下次验证方式**：带变量混合题 `printf("[%d和%d]", (a,7), a+1)`（gcc 实测输出 `[7和3]`，可作对照）＋ 逗号题**换同型**如 `x=(a=1,a+5,a*3)`（⚠️ 原题 `(a++,a++,a++)` 答案已泄露，禁止再用） |
| 🔴 **整数除法截断 —— 09-14 已补讲，明日复查理由** | `printf("%d",7/2)` 他答 **3 ✅ 但理由是"%d 不显示 .5"** → 模型错：刀在除法、不在 printf。同日补讲后用 `int i = 1/2;`（**这行根本没有 printf**）考他 → 答 **0 ✅**，模型实际已建立，只是**嘴上仍用旧说法**。**验证方式（明日）**：问 `7/2` 与 `7/2.0` 差在哪、`8/3` 印几并要求说"刀在哪一步"。课本 p61「两个整数相除的结果为整数，如 5/3 的结果值为 1，舍去小数部分」 |
| ✅ ~~#C006：阶梯题漏看中间那道 `else if`~~ → **2026-09-19 销账** | 09-15 综合题 `int a=7; if(a=0) printf("[A]"); else if(a>5) printf("[B]"); else printf("[C]");` 他**答案栏写 `[A]`、理由栏却推出 `[C]`**（两手打架）→ 漏看中间那道门，直接从第一道门跳到 `else`。当场按未过处理、重答三问 → **1 轮内全对**（0 / 不成立 / `[C]`），与 gcc 实测（`[C]`、`a` 已变 0）完全一致 → 判为**"人肉编译没走完一遍"的流程问题，非概念问题**，但 n=1 最易回生。**下次验证方式**：换同型题 **`int a=9; if(a=0)… else if(a<5)… else…` → 实测 `[B]`、`a=0`**（🔧 2026-09-17 gcc 实测换题：原写 `a>3` 是**判据弱题** —— 把中间门整个删掉也是 `[C]`，**答对不能证明他看了中间那道门**；改 `a<5` 后删门得 `[C]`，**答案不同才能判别**，顺带考「门①已把 `a` 改成 0，门②要用新 `a`」），并**要求他一道门一道门报编号再下结论**。⚠️ 下次出这道题**必须现场 gcc 跑一遍对账**，禁止凭印象报答案。<br>✅ **2026-09-19 复检结果**：他答 **`[B]` + `a=0`**，与 gcc 实测一致；追问两问全对 —— ① 门②里的 `a` 是 **0**（原话「上一个给他赋值为 0」= 门①改写了盒子、门②必须用**新值**，**这正是 09-15 没走到的那一步**）② `else` 不进 =「门②已达成条件打印 `[B]`，就不需要再往下走」。⚠️ **但只报了两道门，是追问后才补全** → 判通过，**"逐道报全"未内化**，移入新观察位。<br>**同批复检 09-15 三课（09-19 全部通过，跨日期）**：① 门卫换同型 `if(-3)` → `[Y]`「0为假其他为真」✅ **升 🟢** ② `=`塞/`==`问 `int b=8; if(b=2)…` → `[P]`/b=2，**变式** `b==2` → `[Q]`/b=8 ✅ **升 🔵 已稳固** ③ 阶梯倒序找错题 `score=92` → 答出「要从大到小排序」+ 实际 `[C]` + 正序应 `[B]` ✅ **升 🟢**（gcc 五路实测含正序对照，全部一致）<br>🔴 **新观察位：流程纪律** —— 09-19 连续 3 题都只报一道门就停笔，追问后才补全。**概念层无缺口，问题在"算出答案就停笔"的习惯**。下次出同型题**不追问**，看他是否主动报完所有门。<br>✅ **同日后续表现**：第四课两轮 6 问**全部主动写了理由**，观察位本轮未复发（但都是单路径题，测不出"报完所有门"）→ 继续挂着。 |
| ✅ **1.4 第四课 嵌套 `if` 与 `else` 配对 —— 2026-09-19 讲完 → 09-20 跨日复检通过，升 🟢** | **规则**：`else` 总是与它上面**最近的、未配对的** `if` 配对（课本 书 p115 / 物理 p132）。<br>**gcc 三行对照表**（本课凭据）：`x=1,y=0` 无括号 → `[2]`；`x=-1,y=0` 无括号 → **空**；同参数**加括号** → **空**。<br>**首考三问全对**：① `else` 配的是内层 `if(b>0)`（他原话「这两相配」）② 印 `[X]` ③ `a` 改成 `-5` → **什么都不印**。🎯 第 ③ 问正是他上一轮**错误模型**（「两个 if 的条件都要完成才走 else」）会答错的地方，这次答对 = **模型真换了**。<br>**变式三问全对**（`var3.c`，gcc 实测 `[P]`/`[Q]`/空）：① `u=5,v=3` 加括号 → `[P]`，理由「两个 if 条件都通过直接打印，不需要再走 else」✅ ② `w=-5,z=3` 加括号 → `[Q]`，理由「`if(w>0)` 条件不通过，**`else` 在花括号外对应的是 `if(w>0)`**」✅ ← **这句是他自己说出来的，正是本课核心** ③ `m=-5,n=3` 无括号 → 空，理由「第一个 `if` 为假无需再向下打印」✅。<br>**验证方式（明天）**：换同型新题（换变量 + 混用加/不加括号），**要求逐层报配对关系再下结论**。<br>🔴 **教练侧失误**：本课前**未讲就直接考**嵌套 `else` 配对 → 用户当场指出「这个知识点要先学吧，我还没学过，只能靠猜测」，**反馈成立**，是**第 2 次**犯「没教过的不出题」（第 1 次 09-09 `for` 循环）。已补讲后重考。<br>✅ **2026-09-20 01:20 首检复检：A 组 2/2 全对** —— 两条配对关系（「走 if 外的 else」→ `[O]`；「走 if 里的 else」→ `[N]`）**都是他主动报的、没追问** → **🔴 流程纪律观察位首次清账**（09-19 连续 3 题都要追问才补全）。⚠️ **但距本课仅约 5 小时、未过夜 → 按自定判据「跨日 = 过了一夜」仍不算跨日，暂不升 🟢**，真正的升格复检放睡醒之后。 |
| ✅ **1.4 第五课 逻辑运算符 `&&`/`\|\|`/`!` —— 2026-09-19 讲完 → 09-20 跨日复检通过，升 🟢** | 课本 **书 p105**（物理 p122）。三个运算符**一次只讲一个**，讲完立刻一题。<br>**首题撞洞**：`int a=4,b=0; printf("%d", a>0&&b>0);` → 答「不打印」❌ → 立 **#C007**（把 `printf("%d", 表达式)` 当成 `if`；缺的是「**表达式有值**」）。<br>**补教一个概念**后重考全对（`expr2.c`：`printf` 版 `0` / `if` 版 `[B]`）→ 他自己总结出「`&&` 符号下**真就打印 1、假就打印 0**」。<br>**`\|\|`**：`a>0\|\|b>0`→`1` ✅（理由「b>0 真、a=0 假，但 `\|\|` 只要一个真」）；`a>0\|\|b<0`→`0` ✅（「两边都为假」）。<br>**`!`**：`!5`→`0` ✅（理由「在 `!` 眼里 5 就是条件真，真再加上 `!` 就为假」）。<br>**真题原型**（`!a\|\|b` 求值两问，`!4\|\|6` / `!4\|\|0`）→ `1` / `0` ✅✅ 理由完整。<br>**验证方式（09-20 白天）**：① 第四课同型题（`else` 配对，换变量 + 混用大括号）② 第五课出一道**「求值」形式**的题 —— 答案必须答 **0 或 1**，答「打印/不打印」= 未过。<br>📊 分量已当场报：2021 单选 1 / 2022 单选 2 / 2023 单选 4 都是这个考点，**3 分/题**。<br>⚠️ **跨日口径**：本场收尾已过 0 点（09-20 00:51），但距第四课仅 **4.5 小时**，按项目「同日连做不算稳」的精神**判为同一场次**，第四课不升 🟢。<br>✅ **2026-09-20 01:20 首检复检：B 组 2/2 全对，且答的是「求值」形式（答案 `1` / `0`，不是「打印/不打印」）** —— B1 答「`!` 反转条件假为真，`&&` 左右都为真 → **1**」；B2 答「反转条件真为假，两边都为假 → **0**」。🎯 **#C007 防回生首战成功**（正是 09-19 答「不打印」的那个位置，这次主动报的是**数值**）。⚠️ 同样因未过夜**不升 🟢**。 |
| ✅ **1.4 第六课 `switch` —— 2026-09-20 01:25 开讲 → 01:50 收口 → 14:30 跨日复检通过，升 🟢**（穿透 / `break` / `default` / 多 `case` 共用 / 物理顺序穿透） | 课本 **书 p117**（物理 p134-135）4.3 利用 `switch` 语句实现多分支选择结构。<br>**画面只用一个**：**电梯直达**（对比 `else if` 阶梯 = 一层一层敲门问）。<br>**两个核心点**：① 括号里放的是**「值」不是「条件」**（`if` 括号里是条件，`switch` 括号里是拿去比对的**值**）② **跳进去后不会自动停、会一路往下滑 —— `break` 就是刹车**。<br>**gcc 凭据**（`sw1.c` / `sw2.c`）：`x=2` 有 `break` → `B`；**`x=2` 无 `break` → `BCD`**；`x=3` 无 `break` → `CD`；**`x=1` 无 `break` → `ABCD`**；case 顺序打乱 `x=2` → `[R]`（顺序不影响结果）；无匹配 `z=9` → `[R]`（走 `default`）。<br>**首题**（顺序打乱版 `x=2`）→ 答 **`[R]`**，理由「输入 2 的值就读取 2 为 `[R]`」✅ 说出了「**按值找格子**」这个核心，非循环论证。<br>**验证方式（睡醒后第一题）**：**穿透题** —— `x=2` 无 `break` 应输出 **`BCD`**。⚠️ **故意不在讲完 10 分钟内考**：那是短时记忆；过夜后考才是长时记忆，诊断价值更高（也是「跨日 = 过了一夜」判据的正面用法）。<br>**09-20 01:40 提前考**（用户拍板「今天直接把 1.4 学完」，穿透题不再等到过夜）：<br>· **T1 穿透**（`x=2` 无 `break`）他答 **`BC`**，gcc 实测应 **`BCD`** ❌ —— **但判不计分**，题面里的 `default` **从没讲过**（教练第 3 次「没教过就出题」）<br>· **T2 多 `case` 共用**（`g=2`）→ 答 **`X`** ✅<br>· **T3 穿透 + `break` 混合**（`c=3`）→ 答 **`CD`** ✅<br>✅ 已补讲 `default`（gcc 七路实测 `default2.c`）+ 讲清「`else` 不穿透 / `default` 会穿透」。<br>**重考题**（`t45.c` 实测）：T4 `n=4` → **`[R][T]`**（`default` 夹在中间被穿透）；T5 `n=9` → **`[T][P]`**（`default` 在最前且无 `break`）。<br>**验证方式（明天）**：同型换数字再考一次 —— 今天这次答对只证明「听懂了」，**过夜后那次才作数**。<br>**09-20 01:45 重考（补讲 `default` 后）**：<br>· T4（`default` 夹中间，`n=4`）→ 答 **`[R][T]`** ✅<br>· T5（`default` 在最前，`n=9`）→ 答 **`[T]`**，实测应 **`[T][P]`** ❌ —— **漏了 `default` 穿透**（只走到 `default` 就停笔，忘了它后面还有 `case` 且**没有 `break`**）<br>⭐ **学生自主实验设计（本轮最有价值的输出）**：他追问「**如果 `default` 和 `case 7` 对调位置，`case 4` 还会穿透到 `default` 吗？因为下面的 `case 7` 有 `break`**」→ **gcc 实测回答：不会**（`ask.c` A2，`n=4` → **`[R][S]`**，`case 7` 的 `break` 把路径截断了）。<br>🔑 **由此得到本课最硬的一条结论**：**穿透跟的是「代码里写的物理顺序」，不是 `case` 数值的大小顺序** —— `case` 倒着写 `7-4-1` 时，`n=7` → **`[S][R][P]`**、`n=4` → **`[R][P]`**（gcc 实测）。讲法用课本原话：**「`case` 常量表达式只是起语句标号作用，并不是在该处进行条件判断」—— 标签不检查**。<br>**重考**：T6（`m=5`，`default` 夹中间）→ `[D][H]`；T6b（`m=1`）→ `[A]`。 |
| ✅ ~~09-14 收官三笔欠账~~ → **09-15 全部销清，不再占首检** | ① 吃换行符换同型 `getchar` 版答对 `a=2.5 / p=回车 / q=X` + 自述「一次只抱一张票」 ② `%5d` 逐字符最终 `<···9>` = **6 字符**全对 ③ `&` 方向性答出「printf 向外取出来不用 `&`」= 真理由（#C005 复习记录第 2 次已签销，约 09-17 随口复查防回生）。📌 顺带订正：本行旧版标注的 `p99` 是物理页，**书 p82**｜下方原题面保留备查 |
| 🆕 **09-14 收官三笔欠账（原始题面备查）** | ① **scanf 吃换行符**（他自报"看不下去"→ 改「点餐台排队票」单一画面重讲后**答对 `a=5 / ch=回车` ✅**，但只隔了一天，**须换同型再考**——原题答案已写进讲义 1.3b 第五节）② **`%5d` 逐字符默写**（`printf("<%5d>",7)` → **7 个字符** `<····7>` = `<` + **4 空格** + `7` + `>`，gcc 实跑核对；宽度=**至少**占几格、超了不砍。⚠️ 本行原写"`<···7>`（3 空格）"是教练笔误，已订正为 4 空格，不记学生错）③ **`&` 的方向性**（他理由写"因为这是 scanf 语句"= **循环论证**，须答出"往里写要门牌号 / 往外读不用"）｜备题：`scanf("%d,%d",&a,&b)` 问"输入 3 4 行不行"（答**不行**，格式串里有逗号必须打逗号，p99） |
| 🆕 **09-14 新教学规矩：每讲一个概念，当场报"考卷分量"** | 他零基础以来第 4 个自主提问是 **「这些全要背吗？这么多含义」** → 他要的不是再多一道题，是**权重**。今后讲完任何一个点，必须同时给「**这条考不考、考就是什么形式**」，不给 = 给他留焦虑。**已实查（非凭印象）**：`grep` 历年真题 2018/19/20/21/22/23/24/26 搜「ASCII / 大小写 / 字符的编码」→ **命中仅 1 条且是数据结构"串"的定义，与 ASCII 无关**；`getchar` 十年只出现 1 次（2022 第 15 题，问"是否从磁盘文件读"，答案 D）。→ **结论：ASCII 编号表不要求背**，只考"编号与字符一一对应"这个观念，题里要用的编号会现给。**只背 5 个**：`\n` 换行 / `\t` Tab / 空格=32 / `'A'`=65 / `'a'`=97（大小写差 32）。 |
| ✅ ~~假掌握复检：赋值覆盖~~ | **09-10 已过关**：合上讲义无表格独立做 → ① `a=a+2; a=a*2;` 答 **10** ✅；② `x=10; x=x-4; x=x+1; x=x*2;` 答 **14** ✅（10→6→7→14，三行连对）→ 同型连对 2 次，「`=` 从右往左 / 覆盖赋值」正式进进度条。错题本 #C001 复习记录已签。09-12 前再抽查一次防遗忘 |
| ✅ ~~新发现的病：答案从上一题借~~ | **09-13 已销账（#C002）**：第三次重做 `for(i=1;i<=3;i++) s=s+i;` 答 **6** ✅，自交过程 `0+1→1+2→3+3`，**没再搬上题的 10**。他反馈「电脑上不好画表格」→ 已确认**一行一轮的写法就算表格**，今后按此交，别被格式卡住。硬判据长期保留：**只给数字不给过程 = 按错题重做** |
| ✅ ~~$x\to0$ 分岔口（已挂 9 天）~~ → **2026-09-19 销账** | **09-19 复检实录**：首答 $\lim_{x \to 2}\frac{1}{x-2}$ 结论「**不存在**」✅ 但理由只说「2-2 等于 0，我也说不清楚」→ 判**结论对、理由未达**（并肯定他"说不清楚"比编一个理由诚实）。<br>讲解只教**一个概念**：左右两侧各奔向哪里（配图 + 数值表）。<br>**立刻练两道答案故意不同的题**：① $\lim_{x \to 3}\frac{1}{x-3}$ → 答「**不存在** + 左 $-\infty$、右 $+\infty$」✅ ② $\lim_{x \to 2}(x-2)$ → 答「**存在**、都趋近 0」✅ **两题全对、理由完整、没搬答案**。<br>🆕 **顺带打掉一个隐患**：第 ② 题两边**符号也不同**（左边 $-0.1$、右边 $+0.001$）却都趋近 0 → 证明判据是「**两边去的终点是否相同**」，**不是「符号是否相同」**。<br>📌 原题面备查：原题 $\lim_{x\to0}\frac1x$ 答案已在演示中泄露，故换同型；🔧 原备选 `lim(x→0) 1/x²` **已废弃**（node 实测左右**都**趋向 $+\infty$、左右一致，考不出分岔）。 |
| ✅ ~~④ 花括号范围题未交~~ | **09-13 已销**：答「两行 AB 一行 C」，内容三件全对（循环 2 轮 / 括号内 A、B 各 2 次 / 括号外 C 只 1 次）。⚠️ **教练侧订正**：本行原写「共 5 行」是错的，gcc 实跑得 `ABABC` = **5 个字符、0 次换行**（`printf` 从不自动换行，只有 `\n` 才换）→ 题面措辞错，不记学生错。已当场补授 `\n` 一课并跑双版本对照 |
| ✅ ~~会读了能不能算出来~~ | 09-09 深夜已验证：3 道欠交题全交全对（① =0 ② =3 ③ 自己写出理由）→ **「会读符号、不会算结果」缺口已闭合**，高数 6 题 **100/100**（同考点 09-09 白天是 50%） |
| ✅ ~~lim 符号是否真懂了~~ | 09-09 已验证：能读 lim/x→∞ 并翻译成人话（符号盲区已消除） |
| ✅ ~~第一个 C 程序能否独立写出~~ | 09-09 已验证：最小框架默写 20/20 全对。⚠️ 但**考场没有在线编译器**，已转入「人肉编译」训练，见上面两条 |
| Anki 40 张首轮 | 用户打开 Anki 实际刷，看板记录完成数（门户已新增 Anki 面板，revlog 数据不认自报） |
| 🔴 **FSRS 开关是否真的点了**（用户 09-10 表示自己去 Anki 开） | 开关位置、代价、风险见 **[Anki-FSRS开关-怎么开与代价](Anki-FSRS开关-怎么开与代价.md)**。**硬验证方式**：下次会话从 `collection.anki2` 副本查 `cards.data` 含 `"s":` 字段的卡数应 **> 0**（09-10 实查 **0/2271** = 仍在跑 SM-2）。不接受"我点了"作为凭据；同时确认期望保留率仍是默认 0.9 没被调高。**🔴 09-12 复验：仍是 0 / 2271 张含 `"s":` → FSRS 没开成，还在跑 SM-2。** 但**这不是当前的要紧事**：SM-2 排期本身没坏（保留率 94.3%），要修的是**手**（困难按太多）和**量**（新词开太快）。FSRS 继续挂账，等词库走上每天 20-30 张的节奏再开才有意义 |
| 每日 4-5h 是否兑现 | 用户自报 + 日志文件；连续 2 天未汇报要追问 |
| C01/C03/C04 概念掌握 | 交错小测（C 语言 5 题）≥70% 算过关，否则标记弱项 |
| 🔴 **课本调用机制是否真被使用**（09-10 建的，别让它变成死文档） | 下次会话**抽查教练自己**：任何 C 语言概念回答若没给「第4版第N页」引用 = 违约。用户可直接问"这句是查的还是背的"。脚本可复跑验证：`node scripts/textbook.mjs 自右而左` 应命中 p84 并打印 `[课本数据源]` |

## 📈 科目进度条

> 📐 **口径 v2**（2026-09-19 修订）
>
> **给两个数，不给一个**：
> - **覆盖率** = 已测考点 / 该科考点总数 → 回答"我**验过**多少"
> - **已过率** = 🟢已过关 / 已测考点 → 回答"验过的里面**过**多少"
>
> **格子三档**（一格 = 该考点**撤掉讲义/表格后独立做对**，有脚手架做对不计入）：
>
> | 状态 | 门槛 | 计入「已过率」 |
> |:---|:---|:--:|
> | 🟡 待复检 | 独立做对 **1 次** | ❌ |
> | 🟢 已过关 | 同型连对 **2 次**，且**跨不同日期** | ✅ |
> | 🔵 已稳固 | 过关后再 **1 道变式题**答对 | ✅（另标） |
>
> **v1 错在哪**（为什么要改）：v1 只给一个百分比，且把 **n=1 的格子也算了进去**。
> 「C 语言 32%」暗示"学了三成"，实际是"8 个格子里 3 个只做过一次"。
> 更离谱的是高数写「8%」时**总共只测过 1 个考点** —— 那是**证据不足**，不是"学了两成"。
> 这与项目自己在 `CONF_CAP={1:0.5,2:0.8}` 里做对的事（"做对一题不能说会了"）是同一个逻辑，
> 只是从前没往"科目级"推。

| 科目 | 覆盖率 | 已过率 | 详情 |
|:---|:---:|:---:|:---|
| C 语言 | **15 / 25 = 60%** | **15 / 15 = 100%** | ⚠️ **数字订正（2026-09-23 01:45）**：本格原写「已过率 **11 / 15 = 73%**」，是 **09-21 升格前的旧值**（11 = 基准 7 格 + ⑨⑩⑪ + ⑦🔵）。09-21 跨日复检后 **⑫⑬⑮ 三格升 🟢 + ⑭ 判过升 🟢**（+4）⇒ 应为 **15 / 15 = 100%**。**09-22 未动 C 语言**（当天块 C 是复检、块 E 是 1.5 收口，均未落账），故此后未再变。⚠️ **注意**：「100% 已过率」指**已测的 15 个考点全过**，不是「C 语言学完了」—— 覆盖率才 **60%**（还有 10 个考点没测）。｜🟢 **7 格**（n≥2 且跨日）：① 最小框架默写 20/20（有 09-07 的 0/20 对照）② 赋值覆盖（`=` 从右往左）连对 2 次 ③ `a++`/`++a` 落地时机（#C003 签销）④ **printf 坑位数 + 逗号两副面孔**（#C004）⑤ **整数除法截断**<br>🟢 **+2 格（09-19 跨日期复检通过）**：⑥ **条件=门卫：只有 0 是假，非 0 全为真**（09-19 换同型 `if(-3)` 答 `[Y]`「0为假其他为真」✅）⑧ **`else if` 阶梯：第一个说"是"就进屋、门槛必须从大到小**（09-19 倒序找错题 `score=92`：答出「要从大到小排序」+ 实际 `[C]` + 正序应 `[B]` ✅）<br>🔵 **+1 格（过关后变式题答对）**：⑦ **`if-else` 两条路 + `=`塞/`==`问**（09-19 `int b=8; if(b=2)…` 三问全对 `[P]`/b=2，**变式 `b==2` → `[Q]`/b=8** 也答对 ✅）<br>🟢 **+3 格（2026-09-20 14:30 跨日复检通过 —— 三格同时升格，`recover.c` gcc 对账）**：⑨ **嵌套 `if` 与 `else` 配对**（R1 `m=5,n=-2` 无括号 → 答 `[Y]` ✅，**补问「`else` 配哪个 `if`」答 B「内层」+ 理由「`else` 靠近谁跟谁」** —— 第四课核心规则自己说出来了）⑩ **逻辑运算符 `&&`/`\|\|`/`!` + 「表达式有值」**（R2 `!a \|\| b>0`（`a=6,b=0`）→ 答 `0` ✅，理由「`\|\|` 只要一个真就印 1，两条件都假印 0」完整）⑪ **`switch` 多分支**（R3 `x=3` → 答 `[C][E][D]` ✅，理由**入口 + 路径 + 终点三段全报**）｜⚠️ ④⑤ 均 n=2 封顶，仍待随口复查｜✅ **#C006 已销账（09-19）**｜✅ 09-14 三笔欠账（吃换行符 / `%5d` / `&` 方向）**09-15 全部销清**｜🟡 **+4 格（2026-09-20 深夜 · 1.5 循环结构 —— 均**同一场次内独立做对**，未过夜，按口径 v2 只记 🟡）**：⑫ **`while` / `do-while` 循环结构**（`while` 先判断后执行 → 可能一次都不跑；`do-while` 先执行后判断 → **至少跑一次、末尾必须加分号**。09-20 判卷题 `i=5; do{printf; i++;} while(i<3)` → 答「印 5、`i=6`、**进 do-while 是先输出一遍**」✅ 核心规则自己说出来，gcc 对账 `do2.c`）⑬ **`for` 三件套 + 末尾分号陷阱**（`for` = 起点/条件/推进收进一行；**括号后不加分号 —— 加了循环体就变成空语句，大括号掉到循环外**。gcc 凭据 `for1.c` F2/F2b：**只差一个分号，100 vs 500**。卡点记录：先说「不懂为什么 s2=100」「s 没加入循环」，两轮把三个数张冠李戴 → 改用「复读机比喻 + 逐格执行表 + 三步机械法」后**无分号版 3/4/3 全对**、**迁移题 4/5/10 全对**（换数字 + 换增量 + 换有无分号三维度同时变））⑭ **`break` / `continue`**（`break` = 整个循环不要了立刻出去；`continue` = 这一圈不要了直接进下一圈。gcc 凭据 `break1.c`：B1 `12` / B2 `1245` / B3 break 出去 `i=3` / B4 正常跑完 `i=6` / **B5 while 里 continue 写在 `m++` 之前 → 卡死**（`m` 永远是 3）/ B6 补 `m++` 后 `1245`。首题 `continue` 求和 → 答 `12 6` ✅，且 `s=12` 是他自己推的（中间值 `1→3→7→12`））⑮ **循环的两个「最后是几」**（**循环体执行几次** = 在循环里 N 次 / 在循环外 **1 次**，没有第三种；**循环变量最后是几** = 正常跑完走**越界值**（`i<=5`→`6`）/ `break` 打断停在**跳出那一刻**（`i==3`→`3`）。这两条能同时解释 `100 vs 500`、`3 4 vs 1 4`、`12 6 vs 3 3`）｜🎉 **09-21 跨日复检（⑫⑬⑭⑮ 四格全抽）→ 三格升 🟢**：**⑫ `while`/`do-while`**（`int i=2; do{printf("%d ",i); i++;} while(i<4);` → 答 `2 3`、i=4 ✅）<br>**⑬ `for` 末尾分号陷阱**（s=100、i=6 ✅，但**漏报第①个数**、表述「大括号不执行」有歧义 → 订正为「**不在循环里**、执行 **1 次**」）<br>**⑮ 循环的两个「最后是几」**（`for(k=1;k<=4;k++) m=m+k; m=m+100;` → 答 `110 5` ✅）<br>⚠️ **⑭ `break`/`continue` 三考才过** —— 第一轮答 `0 6`（**把 break 当成 continue**：以为整个循环体不执行 + 以为循环正常跑完；实际 `6 4`）→ 第二轮答 `15`（**把两行顺序搞反** —— 答的是「`if` 写在 `s = s + a` 下面」的结果；实际 `10 5`）→ **换讲法**（A/B 版本对照 + 逐格表盯「第几步」）→ 第三轮**推理四项全对**（break 终止整个 for / `sum = sum + b` 不执行 / `b++` 不走 / b 停在 7）但**加法失误**（2+3+4+5+6 = 20 算成 21）→ **判过**（过程全对）。⭐ 他自陈疑惑「⑮ 里两个分号让我有点疑惑」→ 用**统一规则**解掉：**`for` 只管紧跟它的那一条语句**（单句 / 大括号块 / 一个分号）→ **⑬ 与 ⑮ 是同一规则的两个面**。📌 **排题漏洞自记**：09-20 只单独考过 `continue`，`break` 只有「读 gcc 输出」的对照题 → 跨日一考就露；**今后 break / continue 各自单独考一次**<br>📍 章节：**1.1 ✅ / 1.3 ✅ 过完；1.2 🟡 部分**（整数除法、逗号表达式已学；**求余 `%` 09-20 已补**、**运算符优先级「算术 > 关系 > 赋值」09-20 已补**；**类型转换仍未学**）；**1.4 六课全下完**（门卫 / if-else / else if 阶梯 / 嵌套 if 与 `else` 配对 / 逻辑运算符 / **`switch` 09-20**），**1.4 全部完结（09-20 14:30）**；**1.5 循环结构 已下五课**（`while` / `do-while` 09-20 → **`for` + `break`/`continue` 09-20 深夜** → **🆕 嵌套循环 09-23 凌晨**），**只剩 1.5-⑥ 综合举例**｜🆕 **+2 格（2026-09-23 凌晨 · 1.5-⑤ 嵌套循环，均记 🟡 未过夜）**：⑯ **同型嵌套**（`for(i=1..4)` 套 `for(j=1..6)` 印 `@`）—— 首答错（`j` 答 8、行数答 5）→ 讲清「**`j++` 跟在循环体后面**，判断失败时没有体就没有 `j++`」→ 重考 **4 项全对**（4 行 / 24 个 / `i=5` / `j=7`，gcc `nest8.c` 实测 `after: i=5 j=7 n=24`）⑰ **变式：内层上限跟着外层变**（`for(j=1;j<=5-i;j++)`）—— 首答「每行 3 个」→ 断点定位 = **只代入了 `i=2` 那一行**（取样偏差，不是概念缺失）→ 给逐行填空表后 **全对**（4/3/2/1、共 10 个、`i=5 j=2`，gcc `nest9.c`）｜讲义沉淀：1.3a、1.3b、**1.4a（选择结构 if，09-15 新建）**、**1.4b（嵌套 if / 逻辑运算符 / switch / 条件表达式，09-20 新建，`docs/posts/computer/notes/1.4b-嵌套if与switch-零基础.md`，自检 8 题全 gcc 实测）**、**gcc 凭据集 `编程技能/零基础教程/gcc-1.5循环/`（for1-4.c / break1-2.c + README.md 预期输出索引，09-20 深夜新建）** |
| 数据结构 | **0 / ?** | **—**（从未测过） | 二轮任务；2.4a/2.5a 专项已备。**0 条日志** → 见 [科学性诊断](./科学性诊断-2026-09-19.md) §5.1。**🆕 09-19 优先度调整**：不再整体等二轮 —— 「**图**」提到数据结构第一优先（真题 4 年 11 题 / 估分 ~20 分 / 数据结构第二高频，而视频覆盖全库倒数第 4 且仅 1 支视频）。**但分两层**：概念层（出度入度 / 多对多 / n−1 边 / O(n²)，占图的 45%）**现在就能做**；算法层（DFS/BFS/Prim/Kruskal/邻接矩阵）**须等 1.6 数组**。依据见 [考点覆盖×真题考频-交叉](./考点覆盖×真题考频-交叉-2026-09-19.md) §3.1 §3.6 |
| 高等数学 | **2 / ?** | **2 / 2 = 100%** | ✅ **极限求值 $x\to\infty$（含抓大头）已过关**：09-10 高数 6 题 100/100（09-09 白天同考点仅 50%）<br>✅ **左右分岔口已销账（09-19）**：挂 **9 天**的欠账清掉 —— $\lim_{x \to 2}\frac{1}{x-2}$ 答「**不存在** + 左奔向 $-\infty$、右奔向 $+\infty$」；对照题 $\lim_{x \to 2}(x-2)$ 答「**存在**、两边都趋近 0」。**两题全对且理由完整**，且**两题答案故意不同却没触发"搬答案"**（node 数值表双向对账，全部一致）<br>🆕 **顺带打掉一个隐患**：第 2 题两边**符号也不同**（左边 $-0.1$、右边 $+0.001$）却**趋近同一个终点 0** → 证明判据是「**两边去的终点是否相同**」，**不是「符号是否相同」**<br>未测：$\frac{0}{0}$ 型、导数依据 |
| 政治 | **0 / ?** | **—**（从未测过） | 三轮任务（12 月起）。⚠️ 但 **11 月底前必须做完题型结构分析**（不需背诵）→ 见诊断报告 §5.3 |
| 英语 | **1 / 待测** | **—**（基线待测） | ⚠️ **09-21 修正：`资料/英语专项/` 不是零使用** —— 09-19 19:34 做过 **2014 全国I卷第 1 篇**（凯霍加河污染）并**自建 21 张卡**（6 张语法 + 15 张生词），09-19 20:10 与 09-20 01:29 各复习一轮（**直读 Anki `collection.anki2` 的 revlog 取证，非自报**）。当时**全程漏登记** → **第 5 次漏账事故**，09-21 已补记。**修正后英语 4 条 / 最近 09-19 / 断 1 天**（原误判「断 8 天 + 163 篇从未使用」）。⚠️ 2014 篇的 8/10 **不能当基线**（做过 + 建卡 + 复习 2 轮 = 回忆）→ 09-21 用 **2015 全国I卷**重测真实水平。Anki 2230 词牌组在刷（**保鲜轨道**）。⚠️ 分值占 **20%** / 投入占 7.9% → 见诊断报告 §5.4<br>🆕 **09-22 补建 2015 篇卡片 17 张**（语法 9 + 生词 8）—— **学生自己想起**「今天学习的语法填空卡片没更新到 anki 上」。走 AnkiConnect 直写（`createDeck → addNotes → findCards` 回读校验 + `notesInfo` 抽查）：**语法牌组 6 → 15 张 / 生词牌组 15 → 23 张**，0 重复。卡片依据：3 个错题（`arriving`→`arrived` / `after`→`before` / `for`→`by`）+ 他主动问的分句谓语 + 非谓语作定语 + 8 张生词。⚠️ **第 6 次「做完但没落账」** —— 机制漏洞是「**做题 → 建卡**」不在收工四件事里，已补进 skill |

## 🧠 弱项画像（<70% 标记）

| 日期 | 知识点 | 得分 | 错误类型 |
|:---|:---|:---:|:---|
| 09-07 | C01 基础语法 | 25% | 程序框架残缺（无法独立写出 main）、整数除法细节（7.0/2 变换不会） |
| 09-07 | 高数-极限符号 | 0% | **符号盲区：不认识 lim/x→ 记号**（数学语言扫盲优先于解题技巧） |
| 09-07 | 高数-求导 | 50% | 会背 (x²)′=2x 但写不出依据（幂函数求导公式） |
| ~~09-09~~ | ~~高数-极限求值~~ | ~~50%~~ → **09-10 已 100%** | **应用错误型：会读符号，不会算结果** —— `lim(x→∞) 1/x` 只把条件翻译成人话就停（没写出"=0"）。✅ 09-07「符号盲区」已补，✅ 09-10 深夜「不会算结果」也已补（见下） |
| 09-09 | C 语言-程序最小框架 | **已掌握 20/20** | ✅ 默写全对（#include / int main() / 大括号 / return 0 / 分号），09-07 的框架残缺已消除 |
| ⚠️ 09-09 | 高数-术语书写 | — | 把「无限」写成「无线」；`printf("wpc ")` 引号内多一个空格 → 今后要求**逐字符自检输出**（考场没有编译器替他报错）。09-10 仍在写错，未改 |
| **09-10** | **高数-极限求值 + 抓大头** | **100%（100/100）** | ✅ 6 题全对：① =0 ② =3 ③ 判断+**自己写出理由**（"2x 无限趋近于无穷大怎么会等于 0"）④ =2 ⑤ =5。下一处风险在 $x\to 0$ 分岔口，**尚未实测** |
| **09-10** | **C 语言-for 循环 / 累计器 vs 计数器** | **0%（0/40）** | ❌ `for(i=1;i<=4;i++) s=s+i;` 答 **4**（正确 10）→ ①`i` 与 `s` 两个盒子混淆 ②以为循环只跑 1 轮 ③把 `i++` 当成"循环在干的事"。**根因在教练：变量/赋值没教就出循环题** |
| **09-10** | C 语言-赋值覆盖（`=` 从右往左） | **✅ 已掌握（连对 2 次）** | 先假掌握（有表格对、撤表格答 6）→ 当晚**合上讲义重做答 10 ✅**，同型新题 `x=10;x=x-4;x=x+1;x=x*2;` 答 **14 ✅**。#C001 已签；09-12 前抽查防遗忘 |
| **09-10** | 🔴 **C 语言-跳过计算 / 答案从上一题借** | **新发现，比前一条更危险** | `for(i=1;i<=3;i++) s=s+i;` 答 **10**（正确 **6**）且**没交表格**——上题 `i<=4` 恰好 10，直接搬答案。→ 立**硬判据：只给数字不给过程 = 按错题重做**，今后每次作业必须交表格。已登记 #C002 |
| 📌 09-10 | 教学纪律（**写给教练自己**） | — | 用户反馈「你是不是忘了我都是零基础」**成立**。五条硬规矩：①一次只教一个概念 ②没教过的不出题 ③他答错先自查是否我跳步 ④**判卷顺序：先看过程、后看数字**——只给数字没过程一律按错题重做（①② 数字对但没过程，仍只记半信，须同型复验）⑤**相邻两题刻意让答案不同**：本轮 ③ 正确答案 6、上题 10，他答 10 当场暴露"搬答案"——答案错开是**抓假会的探针**，不是替他防错 |
| **09-14** | 🔴🔴 **判据有洞：正确率抓不住"听不懂"**（教学侧，不是他的理解力） | 81% 却仍看不懂 | 1.3 八题**数字 5/5 全对**，他随即说「**这一部分还是看不下去，讲得简单点**」→ **该反馈成立**。第五节首版一次塞 **6 层**（两张票 + `%c` 不挑 + 清洁工空格 + `getchar` 扫把 + 三样输入 + `'\n'` 写法），**比喻和术语混排 = 零基础抓不到主线**。推倒重讲、**只留「点餐台排队票」一个画面**后他随即答对。→ **新规：每讲完一节必须主动问「这一节有没有哪句话你看不懂？」，只有他亲口说不懂才算真实反馈** |
| **09-14** | ⚠️ 他用"答对数字"掩盖"理由没懂" | 8 题扣 3 处理由 | ② `printf("%d",7/2)` 答 3 ✅ 理由仍是"打印时砍掉"；⑦ 问为什么 scanf 要 `&` → 答「**因为这是 scanf 语句**」= **复述题目 = 循环论证**。→ **新规：讲理由每题硬性必填，理由循环一律按未过** |
| **09-14** | 🔴 三笔欠账（下次开课首检门槛） | — | ① scanf 吃换行符（**须换同型**，原题答案已进讲义 1.3b）② `%5d` 逐字符默写（`<%5d>`+7 → **`<` + 4 空格 + `7` + `>`**，他从未默过）③ `&` 的方向性（往里写要门牌号 / 往外读不用）。详见 `开新话题/C语言.md`「当前欠账」 |
| ✅ 09-14 | 考点权重焦虑（**已由实查解决**） | — | 他第 4 个自主提问「这些全要背吗？这么多含义」→ 零基础要的不是再多一道题，是**权重**。`grep` 八份真题：`ASCII`/`大小写`/`字符的编码` **命中 0 条**、`getchar` 十年 1 次 → **编号表不背**，只给 5 项最低清单。**新规：每讲一个概念当场报它在考卷的分量** |
| **09-15** | 🔴 **答案栏与理由栏"两手打架"**（新探针，值得升级为常设判据） | 9 题 8 对 | #C006 首答：**答案写 `[A]`、理由却完整推出 `[C]`**（"为假不打印、走其余路线打印[C]"）。→ 说明**理由栏是他真算的、答案栏是手快填的**。这不是不会，是**答完不回读**。🔑 **判据升级：凡答案与理由自相矛盾，一律按未过（不能因"理由对"就放过）**；同时这给了教练一个免费探针 —— 以后**先看两栏是否互证**，比看对错更早抓到"跳过计算"。已当场按未过处理，重答三问 1 轮全对 |
| **09-15** | 🟢 但同一天出现**三个高质量自主输出**（正向记录，防止只记错） | — | ① 第一课自己推出「0 代表假所以不打印」；② 阶梯题看到倒序实测后**自己复述**「顺序错了，第一道 60 这里条件已经通过就直接打印[C]了」；③ 主动区分「昨天 -8 走上面、今天 -8 走下面」是因为**门卫问的问题不一样**（这是他今天最硬的一步：从"看数字"升级到"看括号里那道题成不成立"）。→ **1.4 三课的概念层是真懂了，风险全在"流程纪律"（走完每道门 + 答完回读）** |
| 📌 09-15 | 教学纪律补充（**写给教练自己**） | — | ① 用户拍板「**一天可交很多新概念，但一次只交一个**」→ 执行到位（一概念→一题→过了才给下一个），本场无一次铺多题；② 沉淀时又**凭印象写了 `switch` 书 p120**，回课本 grep 实测是 **书 p117（物理 p134）** → 再次证明：**页码和答案一样必须当场取凭据，"我记得目录里是"不算数**；③ `else if` 阶梯的"倒序陷阱"是我主动 gcc 双跑试出来的，**不是课本给的** → 这类"跑一下就知道"的对比实验成本极低，今后**新课主动多做一组反例**，比多讲十句有用 |
| 📌 **09-19** | 🔴 **教学纪律（写给教练自己）：第 2 次犯"没教过的不出题"** | — | 1.4 第四课「嵌套 `if` 的 `else` 配对」**还没讲就直接出题考他**，被他当场指出「**这个知识点要先学吧，我还没学过这个知识点，只能靠猜测**」——**反馈成立**。<br>**这是第 2 次同类错**（第 1 次 09-09：变量/赋值没教就抛 `for` 循环，当时他回「你是不是忘了我都是零基础」）。<br>→ 纪律重申：**先讲，再考。出题前先自查一句"这个概念我讲过没有"**。<br>🎯 他答案 `[2]` 是**蒙对的**，模型错（"两个 if 的条件都要完成"）—— 正好又一次证明「**蒙对不等于懂**」，也说明**不教先考出来的分数没有诊断价值**。 |
| **09-19** | 🟢 **同一天的收尾：补讲后重考，6/6 全对，模型真换了**（正向记录） | 6/6 | 被指出「没教过」后，当场只教**一条**规则（`else` 配最近的未配对 `if`，书 p115）+ gcc 三行对照表，然后**重考**：首考三问全对、变式三问全对。<br>🎯 **两处证据说明这不是蒙的**：① 首考第 ③ 问（`a=-5` → 什么都不印）正是他上一轮错误模型会答错的位置；② 变式第 ② 问他**自己说出**「`else` 在花括号外对应的是 `if(w>0)`」= 抓住了「大括号把 `else` 从内层拉到外层」这个本课核心。<br>📌 与上一行的对照值得写进方法：**同一个知识点，"不教先考"得 `[2]`（蒙对）、"教完再考"得 6/6 且理由自洽** —— 这正是「没教过的不出题」这条纪律的实证价值，不是教条。 |
| **09-19** | 🔴 **新洞：把 `printf("%d", 表达式)` 当成 `if`**（#C007，1.4 第五课首题） | 答「不打印」 | 题：`int a=4,b=0; printf("%d", a>0 && b>0);` 问印几 → 他答「**不打印**」，理由「b=0 为假，一个为假 `&&` 不打印；a 大于 0 为真」。<br>✅ **逻辑推理三条全对**（b>0 假 / a>0 真 / 一个假就整体假）→ **`&&` 的运算规则没问题**。<br>❌ 错在**最后一步动作**：这道题里**没有 `if`**，`printf` 一定执行，印的是**表达式的值 `0`**。「不打印」只发生在 `if` 里。<br>🎯 **缺的是新概念「表达式有值」**：`a > 0` 本身就是 1 或 0，可以当数字直接印。已教（含 gcc 五路对照：同一式子 `4>0&&0>0` 放进 `printf` 印 `0`、放进 `if` 印 `[Y]`）。<br>📊 **真题分量（实查）**：2021 单选 1「求 `!a\|\|b` 的值」→ 1；2022 单选 2「`(a==6)\|\|(a!=6)` 的值」→ 1；2023 单选 4「`a&&b\|\|c` 为真」→ D。**几乎每年必考，3 分/题，题干固定问「求值」，答案只可能是 0 或 1**。<br>✅ **同日重考两问全对**（`expr2.c`：`printf` 版答 `0`、`if` 版答 `[B]`，与 gcc 实测一致）。🔑 他原话「这里的 `&&` 符号下**真就打印 1、假就打印 0**……题1 就 `0`，下面走 `else` 条件」→ **自己把「表达式的值就是 0 或 1」总结出来了**；两问答案故意不同也没答成一样 = 真分清了「`printf` 取值」与「`if` 选路」。⚠️ n=1，跨日复检后才升格。 |
| 📌 **09-20** | 🔴🔴 **教学纪律：第 3 次犯「没教过的不出题」—— 这次是 `default`** | — | 4.3 `switch` 收尾三题里，T1 用了 `default`，**但 `default` 从没正式讲过**（只在 gcc 凭据里一笔带过）。他主动提问：「**`default` 还没有详细介绍这个命令是什么，和 `else` 差不多是吗**」—— **反馈完全成立**，是**第 3 次**同类错（第 1 次 09-09 `for` 循环、第 2 次 09-19 嵌套 `if` 配对）。<br>🎯 **T1 他答 `BC`、漏 `D`，根因就是没学过 `default`** → **判该题不计分**（不教先考出来的错没有诊断价值，这条 09-19 已经立过）。<br>→ 纪律再重申：**出题前自查一句「这个概念我讲过没有」**。三次都栽在同一处，说明光写纪律不够 —— 今后**出题前把题面里的每个关键字在「已过关」清单里点一遍**。<br>✅ 补讲后给了 gcc 七路实测（`default2.c`）：省略 `default` 且无匹配 → 空；`default`+`break` → `D`；`default` 无 `break` 在最后 → `D`；`default` 在前+`break`、匹配到 `case` → `A`；`if/else` 对照 → `D`（`else` 不穿透）；`default` 当标点 `x=9` → `C`（**`default` 会滑进 `case`**）；字符型 `grade='F'` → `error`。**核心区别一句话：`else` 是条件的一部分、走完就出结构；`default` 是标号、会跟着穿透往下滑。** |
| 📌 **09-20** | 🔴🔴🔴 **教学纪律：第 4 次犯「没教过的不出题」—— 而且一道题踩两个** | — | 4.4 综合题 T7（闰年）题面里用了 **`!=`** 和 **`%`（求余）**，**两个都没讲过**。他当场指出：「**`!=` 这个是什么意思没有教学过**」—— **反馈成立**。<br>📋 证据（不是印象）：① `!=` 属 4.1.2 关系运算符，**整节没系统讲过**（只在第二课顺带提过 `==`）；② `%` 看板 1.2 行**自己写着**「类型转换、运算符优先级、**求余 `%` 未学**」。<br>⚠️ **最刺眼的一点**：上一轮（01:45）我刚立下「**出题前把题面里每个关键字在『已过关』清单里点一遍**」，紧接着就违反了 → **立规矩 ≠ 执行规矩**。<br>→ **T7 判作废**（不作错题登记）。已补讲**关系运算符全表**（6 个，含优先级阶梯「算术 > 关系 > 赋值」，gcc 七路实测 `rel.c`）+ **求余 `%`**（`7%3`→1、`8%4`→0；闰年三余数 `1900%4`→0、`1900%100`→0、`1900%400`→300）。<br>📌 **四次同类错**：09-09 `for` 循环 / 09-19 嵌套 `if` 配对 / 09-20 `default` / 09-20 `!=`+`%`。**频率在加快**，说明靠自觉不行 —— 出题前必须**机械地**过一遍符号清单。 |
| **09-20** | 🟢 **同轮正向记录：穿透机制 + 多 `case` 共用，他真懂了** | 2/3 | T2（多 `case` 共用，`g=2` → `X`）✅、T3（穿透 + `break` 混合，`c=3` → `CD`）✅ —— **两题全对，且理由「没有 `break` 就会继续输出下面内容」正是穿透的本质**。<br>⚠️ 但他的表述有一处错：「T1 T2 有 `break`」—— **实际 T1 没有 `break`、T2 的 `case 2` 才有**，已当场纠正。这说明**他能用规则推结果，但读代码时对 `break` 的位置扫得不够细** → 新观察位：**读代码时逐行标出 `break` 的位置再下结论**。 |
| **09-23** | 🔴 **E56 新洞：`for` 的「推进表达式」在判断失败后是否还会跑** | 首答 `j=8`（正确 **6**） | 1.5-⑤ 嵌套循环首题（`for(j=1;j<=6;j++)`）他答「`j` 最后是 8」，自述原话：「**我不知道为什么 `j` 没有达到前面的条件但是还是会运行 `j++` 的指令，这里就成了 7**」。<br>🎯 **根因**：他的模型是「**判断失败之后还会再跑一次 `j++`**」。**正确模型**：`for` 顺序 = `初始化 → 判断 → 体 → j++ → 判断 → 体 → j++ → … → 判断(假) → 退出` —— **`j++` 跟在「循环体」后面**，判断失败时**没有体 ⇒ 没有 `j++`**。<br>✅ **讲清后重考 4 项全对**（`i=5 j=7 n=24`，gcc `nest8.c`）。<br>📌 **判据**：给 `for(j=1;j<=N;j++)`，问「循环结束后 `j` 是几」→ 必须答 **N+1** 并说出「判断失败时没有 `j++`」。 |
| **09-23** | 🔴 **E58 新洞：内层上限是含外层变量的表达式时，只代入一行** | 首答「每行 3 个」（正确 4/3/2/1） | 变式题 `for(j=1;j<=5-i;j++)` 他答「每行 3 个、4 行、共 12 个」。<br>🎯 **根因定位精确**：他**其实算对了 `i=2` 那一行**（5 − 2 = 3）—— **错在只代入了这一行就当成全部**。**这是「取样偏差」，不是「不会代入」、更不是「不懂嵌套」**（他答对了行数和 `i=5` ⇒ **嵌套骨架是通的**）。<br>✅ **给「逐行填空表」后一遍全对**（4/3/2/1、共 10 个、`i=5 j=2`，gcc `nest9.c`）。<br>📌 **判据**：出「内层上限含外层变量」的题时，**必须强制他先填逐行表再报数** —— 填表本身就消除了取样偏差。 |
| **09-23** | 🟢🟢 **同轮正向记录（很重要，防止只记错）：他的「词性直觉」是对的** | 词性判断 3/4 | 英语第 3 篇词性判断题（`a very ___ tool` / `finished the work ___` / `your ___` / `at ___`）：第 1、3、4 空**全判对**（形容词 / 名词 / 名词）。<br>更关键的是**在真题卷里**：第 3 空他说「这里是他们的**能力**的意思」= **知道 `their` 后面要名词** ✅；第 9 空他说「**自然的**是要修饰后面的名词」= **知道修饰名词要用形容词** ✅ —— **两处词性都判对了，只是不知道 `ability` / `natural` 怎么写**。<br>→ 🔑 **结论：他不是「不懂语法」，是「句法直觉有了、词形变化没学过」= 纯知识缺口。** 09-21 那次 3 个错也全是知识缺口 ⇒ **两次都是知识缺口为主，不是粗心** ⇒ **行动方向 = 补词形转换，不是再讲语法**。 |
| **09-23** | ⚠️ **词形转换缺口（新类别，单独列）** | — | 已实测不会变的：`able → ability`（-le → -ility）、`nature → natural`（名词 + -al）、`kind → kindness`（他写 `kinding`，**用了错的规律**）。<br>📌 **已给的三条规律**：① -le 结尾常变 -ility（able/ability · possible/possibility）② 名词 + `-al` = 形容词（nature/natural · person/personal · nation/national）③ 形容词 + `-ness` = 名词（kind/kindness · happy/happiness · dark/darkness）。<br>📌 **待补的缺口（本次未讲）**：`enough + to do`（第 6 空）、`how + 形容词`（第 10 空）、固定搭配 `at the same time`（第 7 空）、**非谓语动词作定语 vs 谓语**（第 1 空 —— **已列为下一课**）。 |

## 🗂️ 快速索引（教练开工即用的入口）

- 出题依据：`docs/posts/computer/notes/3.1~3.6 专项` + `历年真题/*/考点拆分/`
- 讲解依据：`资料/考点资源图谱-计算机.md`（视频推荐唯一依据，禁凭记忆推荐）
- 教材锚点：**C 语言 → 先查课本原文（见下条脚本）**；高数 B站宋浩、政治徐涛（这两科**无课本全文**，答案须标 ⚠️ 推算）
- 🆕 **📕 课本原文优先机制已落地（09-10，用户要求「遇到知识问题调用原课本来思考」）**：
  - **调用入口**：`node scripts/textbook.mjs <概念词>` / `--page N` / `--pages A-B` / `--toc` / `--ch N`（仓库内脚本，只读，秒级返回）
  - 输出自带**可信度标注**（`✅ 散文` / `⚠️ 含代码` / `⚠️ 含公式(可能乱码)`），并在末尾打印 `[课本数据源]` 路径作为**取证凭证**（防止凭记忆假装查过）
  - 数据源两份副本自动查找：仓库 `guangdong-zhuanshengben-resources\计算机基础与程序设计\《C语言程序设计》.txt`（**已确认被 .gitignore 拦住，不进 git**）→ 桌面原件 → 环境变量 `TEXTBOOK_TXT`
  - **硬协议已写进 `skill:study-coach`**（真实加载路径 `AppData\Roaming\reasonix\global-workspace\.reasonix\skills\study-coach\SKILL.md`）：讲概念/判卷/记忆冲突/无命中四个时机**必须查**，引用一律给「第4版第N页」，禁止编造页码
  - 已实测锚点：**p81**「分号是语句中不可缺少的组成部分，而不是两个语句间的分隔符号」；**p84**「赋值运算符按自右而左结合」（正是 #C001 出处）
  - 三条红线不变：①禁止复制其中代码（右花括号少 142 个）②含公式题干须回原 PDF 核对（4.3 已被抽取毁）③有版权不进 git，只登记页码指针。**用法与硬伤必读 [[谭浩强C语言第4版-txt体检与用法]]**
- Anki 排期算法开关说明：[[Anki-FSRS开关-怎么开与代价]]（SM-2 vs FSRS、代价与风险、验证方式；**AI 不代改 Anki 设置**）
- 学习事件流水账：`备考计划/学习日志.jsonl` → 门户「🪞 复盘」页读它（`GET /api/log`）
- 备考时间线：[[2027届-完整备考时间线]] · 自测表：[[进度自测表]]
- 🆕 **考点库（B 站课程侧，2026-09-19 建）**：[[考点库-跨视频合并-2026-09-19]] —— **79 份**提取产出的跨视频汇总，覆盖 **18 支**视频，按官方 21 模块 / 高数 12 章归位。**✅ 提取已完成 18/18，不再是移动靶**。**刷新方式（三个 flag 一起给，否则门户面板与搜索会对不上）**：`node scripts/bili_merge_extract.mjs --md 备考计划/考点库-跨视频合并-2026-09-19.md --json data/bili-analyze/_extract/_merged.json --coverage data/bili-analyze/_extract/_coverage.json`
  - ⛔ **「还差 6 支」是假缺口，已关闭**：`data/bili-analyze/` 下有 24 个目录，其中 6 个是抓错的垃圾 —— BV176gY6nEb3（蚂蝗纪录片）/ BV1gh9MBkEEC（音乐视频）/ BV1PRM26hEbk（漫威票房解说）/ BV1TDj36dEdc（VIVO 手机测评）/ BV1xzBCBFExz（电影解说）/ BV1rg411h7Z1（惠普笔记本维修合集）。**每段 `_verify` 都是 `ok:false`**，校验器早已拒收、提取也正确地跳过了。**不是待办，不要再去提取它们**。产物里 `knownBv` 已改为只数「至少有一段 ok:true」的视频，并单列 `rejectedBv`。
  - 📊 **已接进 Studio 门户**：侧栏「📊 考点库」页（第 8 个 tab）。读 `_coverage.json` 出模块表 / 缺口榜 / 视频分层，搜索走 `/api/kaodian/search`（索引 `_merged.json`）。**门户只做导航与判读，知识点正文仍在笔记库**（AGENTS.md §9 明禁复制）。
  - 🔧 **2026-09-19 修**：门户「笔记站」视图此前一直显示 404 页 —— iframe 用了别名前缀 `/zhuan-sheng-ben-notes/`，而 VitePress 的 `base` 是 `/darling016123/`，客户端路由剥不掉前缀 → 渲染 `PAGE NOT FOUND`（HTTP 仍 200，只有一句 hydration 警告）。已改为 `/darling016123/`，并给 `check-view.mjs` 加了 iframe 404 守卫。
  - 🔧 **2026-09-19 修**：门户的「⚠️ 提取仍在跑」是陈旧值（写进 JSON 的生成瞬间读数）→ 已改为服务端读时现算。
- 🆕 **考点覆盖 × 真题考频（2026-09-19 建）**：[[考点覆盖×真题考频-交叉-2026-09-19]] —— 把「视频有没有人讲」和「真题打在哪」叠起来。**最重要的结论：⑰图（真题审计 11 题 / 估分 ~20 分）视频覆盖全库倒数第 4 且只有 1 支视频在讲；⑩文件同理（真题 6 题 / 视频 11 条）**
- 🆕 **🔥 高强度学习方案（2026-09-20 建）**：[[高强度学习方案-2026-09-20]] —— **核心只有一个数：把「推进日密度」从实测 23% 提到 50%**。含每日五块结构（新内容块排最前、不可被挤掉）、四阶段目标与最低密度门槛、干扰控制四条、五个手工可记的衡量指标。**依据是实查数据**：13 天里只有 3 天在推进新内容、速率 0.667 模块/推进日、剩余 29 模块、倒计时 162 天。⚠️ 方案生效的唯一标志 = 密度真的 ≥50%，**方案本身不算学习量**。
- 🆕 **🌙 自由作息与考前切换方案（2026-09-20 建）**：[[自由作息与考前切换方案-2026-09-20]] —— 用户日常**凌晨 3–4 点入睡**、要求作息不固定。方案把「自由」定义成 **T0 锚点式**（结构固定、时刻随你），红线只有一条：**起床时刻每日浮动 ≤2 小时**。考前 4 周按**每周前移 1–1.5 小时**（生物钟前移的安全上限）从 03:30 切到 23:00。⚠️ 同时订正两处事实：**① 是 4 科 500 分（计算机专业综合课 200 分 / 150 分钟，另三科各 100 分 / 120 分钟），不是 5 科；② 2026 年实际考试是 3 月 28–29 日 → 2027 年大概率 3 月最后一个周末，比本看板用的 `2027-03-01` 基准晚约 26 天**（基准不改，保守无害，但要知道有缓冲）。
- 🆕 **科学性诊断（2026-09-19 建）**：[[科学性诊断-2026-09-19]] —— 研究设计 / 方法选择 / 数据处理 / 论证逻辑四维诊断，含 24 条可判定评估标准
- 🆕 **B 站视频管道诊断（2026-09-19 建）**：[[B站视频管道诊断与内容可信度-2026-09-19]] —— 调参 12.4x、junk 污染 8.2%、内容可信度分层
- 🆕 **预注册模板**：[[预注册/_TEMPLATE]] —— 每轮开课前填 4 行（改判据必须留痕，防 HARKing）

---

> 🤖 **给下一个 AI 会话的交接指令**：读完本文件后，1) 以「上次会话完成的事」和「本周任务」为上下文开场；2) 优先处理「待验证区」；3) 会话中的学习事件（做题/批改/新弱项）在结束前写回本文件并更新 `last_updated`。
