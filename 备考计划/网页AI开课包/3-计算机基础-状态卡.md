# 🟨 计算机基础 · 状态卡

> **用法**：贴完 `0-教练规则` 后，把**本文件全文**粘贴进去 → 打「开始」。
> **本文件是快照，每上完一节课都要更新。** 快照日期：**2026-09-17**

---

## 一、本话题管什么（**别越界**）

广东专升本「计算机基础与程序设计」：**200 分 · 150 分钟**（考试院口径）。
卷面 = **C 语言（约 100 分，考纲 ①–⑪）** + **数据结构（约 100 分，考纲 ⑫–㉑）**。

⚠️ **「计算机基础理论」不是独立分值块** —— 它藏在 **考纲 ①② 和 ⑪** 里，也就是 C 语言那半边的开篇。

**本话题只管这些理论部分**：进制、单位、存储、硬件、网络、编译链接。
**C 语言语法请去「C 语言」话题** —— 两边交错推进，**别合并成一场**。

> 🔧 **更正记录（2026-09-17）**：本卡原写「一张卷 **100 分**」，**是错的**。
> 反证很直接：四科总分 **500**（政治 100 + 英语 100 + 高数 100 + 计算机 **200**），
> 你的目标分是 **410** —— 若计算机只 100，总分只有 400，**410 根本不可能存在**。

### 🗺️ 全程地图（**「下一课是什么」只看这里，不要猜**）

> **判定规则**：从上往下扫，**第一个 🔴 或 ⬜ 就是下一课**；账没清完不往下走。
> **范围**：本话题只收**计算机基础理论**（下表 5 块），**不含 C 语言语法**。

| # | 板块 | 具体内容 | 状态 |
|:--:|:--|:--|:--:|
| ① | 计算机系统组成 | 硬件五大件（运算器/控制器/存储器/输入/输出）· CPU / 内存 / 外存的区别 · 软件分类 | **🔴 进行中**（五大件待闭卷默写） |
| ② | 数据在计算机中的表示 | 进制转换（二/十/十六）→ 编码（ASCII、原码反码补码）→ **单位换算（×8 vs ×1024）** | **🔴 进行中**（单位换算待复检） |
| ③ | 程序从编写到运行 | 编译 → 链接 → 运行；三类错误的区别（**2026 新增考点**） | ⬜ |
| ④ | 计算机网络基础 | 网络分类 · IP / 域名 · 常见协议 | ⬜ |
| ⑤ | 数据结构基础概念 | 逻辑结构 / 存储结构 / 算法特性 —— **桥接到「C 语言」话题的 2.1** | ⬜ |

**①② 内部的课级位置**：

| 课 | 内容 | 状态 |
|:--:|:--|:--:|
| ①-1 | 硬件五大件 | **🔴 待闭卷默写**（上次 4/5，且有脚手架，不签过关） |
| ②-1 | 字节 ↔ 位（×8，同一层换说法） | 🔴 待复检 |
| ②-2 | 单位阶梯（×1024，上台阶） | 🔴 待复检 |
| ②-3 | 256 种为何最大 255 | ✅ 09-13 答对（n=1，不算掌握） |
| ②-4 | 进制转换 | ⬜ 待查 |

> 📌 **课本原文你不用猜、也不要编** —— 我查好贴给你（`node scripts/textbook.mjs --page N`）。

## 二、🔴 本科最坑人的地方：两条路容易走混

| 走法 | 用在 | 倍数 |
|:--|:--|:--:|
| **换单位**（字节 ↔ 位） | **同一层**，只是换个说法 | **×8** |
| **上台阶**（B → KB → MB → GB → TB） | **跨一层** | **×1024** |

**我 09-13 首考 5 题错 2，就是把 ×8 当成上台阶** —— `5 KB` 我答成 **40**，正确 **5120**。

> 💡 **讲这个时请先只给一个画面**：×8 是「把一个大箱子拆成 8 个小格」（同一层换个说法）；
> ×1024 是「上楼梯，上一层要乘 1024」。**两个画面别同时给。**

## 三、🔴 首检 / 复检（**先清这个再开新内容**）

### 单位换算（约 09-16 已到期，n=2 封顶不算稳）

**必交格式：写全每一步，只给数字 = 按错题重做。**

```
3 MB = 3 × 1024 × 1024 字节 = 3145728 字节
3 MB = 3145728 × 8 位 = 25165824 位
```

**同型复检题**（挑 2–3 道，**答案要错开**，防我搬上一题）：

| 题 | 答案 | 考点 |
|:--|:--|:--|
| `5 KB = ? 字节` | **5120** | 上台阶 ×1024 |
| `5 字节 = ? 位` | **40** | 换单位 ×8 |
| `2 MB = ? 字节` | **2097152** | 上台阶 ×1024 |
| `1 字节 = ? 位` | **8** | 换单位 |

> ⚠️ **`5 KB` 和 `5 字节` 要连着出** —— 这两道的答案 5120 和 40 差 128 倍，
> 是专门抓「把 ×8 当成上台阶」的探针（就是 09-13 那个错）。

### 硬件五大件默写

**标准答案**：**运算器、控制器、存储器、输入设备、输出设备**

⚠️ 上次 4/5，第五件是在选项里挑对的（**有脚手架，不签过关**）→ 这次**闭卷默写**，不给选项。

> 两个易错点先摆出来，别等我踩：
> - **显卡不算第五类** —— 它是**输出设备**里的一张卡，重复计数
> - 「运行内存」就是**内存本身**，不是新类别

## 四、已考过的点与结果（09-13）

| 考点 | 结果 | 判定 |
|:--|:---:|:--|
| 字节 ↔ 位 / 单位阶梯 | 首考 3/5 → 当天订正 2/2 | ⚠️ n=2 封顶 80%，**约 09-16 复检** |
| 256 种为何最大 255 | 1/1 | ✅ 过关（但 n=1 封顶 0.5，**不算掌握**） |
| 硬件五大件默写 | 4/5 | ⚠️ 有脚手架，**不签过关** |
| 进制 / 位概念 | 部分 | 待查 |

## 五、必背反直觉点（**考判断题爱考**）

- `1K` 实际是 **2¹⁰ = 1024**，**不是 1000** —— 但「常把 1KB 说成 1000B」这句话本身**是对的**（考点就在这）
- **一字节 = 8 位**，能表示 **256 种**花样，但**最大数是 255** —— 因为**从 0 开始数**
- **ASCII 占 0~127（7 位）**，所以一个字节刚好存一个西文字符
- **`printf` 从不自动换行**，只有 `\n` 才换行（跨科易错点）

## 六、取证方式（**不用编译器，靠换算式自证**）

**要求我把每一步算式写出来**，例：

```
3 MB = 3 × 1024 × 1024 字节 = 3145728 字节
```

**只给数字 = 按错题重做。**

概念题可以查课本（我去本机查好贴给你）：

```
node scripts/textbook.mjs 字节        # 查概念
node scripts/textbook.mjs --page 36   # 查指定页
```

**课本**：计算机基础用课本（第 4 版，已查证过 p36 / p39 等锚点）。引用要标页码。

## 七、下次开课顺序

1. **清单位换算复检**（第三节，`5 KB` + `5 字节` 连着出）
2. **闭卷默写硬件五大件**（不给选项）
3. 才开新内容 —— 按考纲 21 模块推进（进制 → 存储 → 硬件 → 网络）

## 八、本地文件（**你看不到，需要内容我会贴给你**）

| 文件 | 里面是什么 |
|:--|:--|
| `docs/posts/computer/notes/0.0-计算机基础理论.md` | 基础理论笔记 |
| `docs/posts/computer/notes/1.2-数据的存储与运算.md` | 存储与运算（进制、单位） |
| `docs/guide/2026考纲全解.md` | 官方 21 模块考纲 |
| 谭浩强《C 程序设计》第 4 版 txt | 课本全文（用脚本查） |

## 九、禁止项

- ❌ **不要建议我做工具 / 脚本 / 面板**
- ❌ **不要编页码** —— 课本命中才写「第4版第N页」，无命中标「⚠️ 推算」
- ❌ **不要说「这个很简单」** —— 我是零基础
- ❌ **不要把 C 语言内容拿到本话题教** —— 去「C 语言」话题
