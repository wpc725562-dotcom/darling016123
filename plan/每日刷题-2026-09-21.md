# 📅 每日刷题计划 · 2026-09-21

> 由 `scripts/daily-drill.mjs` 生成。共 **15** 题：基础 14 / 中档 1 / 拔高 0。
> 难度配比 基础:中档:拔高 = 6:3:1，题库与顺序随机（科目交错混排）。
> **零基础正确用法**：读题 → 想 30 秒 → **照抄完整解答 → 合上默写一遍**。做不出来是正常的，不要硬憋，也不要因为不会就跳过。

### 1. 【高等数学】【极限·等价无穷小替换】 ★ 基础

求极限 $$\lim_{x \to 0} \frac{1 - \cos 2x}{x^2}$$

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

当 $x \to 0$ 时，$1 - \cos 2x \sim \frac{1}{2}(2x)^2 = 2x^2$：

$$
\lim_{x \to 0} \frac{1 - \cos 2x}{x^2}
= \lim_{x \to 0} \frac{2x^2}{x^2}
= 2
$$

**这题在考什么**：

等价无穷小替换，$x \to 0$ 时 $1 - \cos x \sim \frac{1}{2}x^2$


</details>

### 2. 【计算机】【C语言部分】 ★ 基础

以下程序输出是（ ）

```c
int a = 10;
int *p = &a;
int **pp = &p;
printf("%d", **pp);
```

A. 10　　B. 地址值　　C. 编译错误　　D. 运行时错误

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：A

**解析**：

`pp` 是指向指针的指针（二级指针）。`*pp` 得到 `p`（指向 `a` 的指针），`**pp` 得到 `a` 的值，即 10。

二级指针就像"门牌号的门牌号"——`a` 是房子里的人，`p` 是房子的门牌号，`pp` 是写着"门牌号在哪"的纸条。一级一级查下去：查纸条 → 找到门牌号 → 找到人。


</details>

### 3. 【高等数学】 ★ 基础

`x → 0⁺` 和 `x → 0⁻` 有什么区别？

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

- `0⁺`：**从右边靠近 0**（比 0 大一点点），如 0.001
- `0⁻`：**从左边靠近 0**（比 0 小一点点），如 -0.001

区别在 `1/x` 上特别明显：`x→0⁺` 时 `1/x → +∞`；`x→0⁻` 时 `1/x → -∞`。

**左右极限不相等 → 极限不存在**，这是判断题最爱考的点。


</details>

### 4. 【计算机】【数据结构部分】 ★★ 中档

哈希查找中，解决冲突的两种主要方法是（ ）

A. 开放地址法和链地址法　　B. 顺序查找和折半查找　　C. 冒泡排序和快速排序　　D. 先序遍历和后序遍历

> 🧭 **先想 30 秒**：先在草稿上画出结构，再算 —— 这科靠画不靠想。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：A

**解析**：

哈希表解决冲突的两种主要方法是开放地址法（线性探测、二次探测等）和链地址法（拉链法）。

哈希表就像"按姓名首字母分信箱"——但可能两个人姓的首字母一样（冲突）。开放地址法：如果 A 的信箱被占了，就放到旁边的空信箱。链地址法：每个信箱里放一个"小篮子"，可以放多封信。


</details>

### 5. 【C语言】 ★ 基础

`int a[5];` 是什么？数组下标从几开始？

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

声明一个能放 5 个整数的数组：`a[0]` ~ `a[4]`。

**下标从 0 开始**，所以 5 个元素的最大下标是 **4**。

访问 `a[5]` 是**数组越界**（第 6 个不存在），编译不报错但结果是垃圾值 —— 考纲里的经典陷阱。

`int a[5] = {1, 2, 3, 4, 5};` 里 `a[0]=1, a[4]=5`。


</details>

### 6. 【C语言】【编程题】 ★ 基础

判断一个数是否为素数

```c
int main() {
    int n, i, flag = 1;
    scanf("%d", &n);
    for (i = 2; i * i <= n; i++)    /* 只需试到 √n */
        if (n % i == 0) { flag = 0; break; }
    if (flag && n > 1) printf("是素数\n");
    else               printf("不是素数\n");
    return 0;
}
```

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：以 n=17 为例，i 试到 2、3、4 后结束，输出"是素数"。

**解析**：

① 循环边界写成 `i < n` 也能对但效率低，写成 `i <= n/2` 同理；② n=1、0 必须判"不是素数"（1 不是素数，真题爱挖这个坑）。


</details>

### 7. 【计算机】【C语言高频简答题】 ★ 基础

什么是指针？什么是指针变量？它们的关系？

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

- **指针**：内存单元的地址，是一个常量性质的地址值。
- **指针变量**：专门用来存放地址（指针）的变量，其值是另一个变量的地址。

**关系**：
- 指针变量通过存储某个变量的地址来"指向"该变量。
- 通过指针变量可以间接访问其所指向的变量的值（`*` 运算符）。
- 定义：`int *p = &a;` — p 是指针变量，&a 是变量 a 的地址（指针），p 指向 a。


</details>

### 8. 【计算机】【C语言部分】 ★ 基础

以下程序输出是（ ）

```c
char s1[] = "abc";
char s2[] = "abc";
printf("%d", strcmp(s1, s2));
```

A. 0　　B. 1　　C. -1　　D. 随机值

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：A

**解析**：

`strcmp` 比较两个字符串的内容是否相等。因为 `s1` 和 `s2` 的内容都是 `"abc"`，所以返回 0（相等）。

`strcmp` 就像"逐字对照检查"——一个字一个字地比，全部一样就返回0。不相等时返回第一个不同字符的差值（正数表示s1更大，负数表示s2更大）。


</details>

### 9. 【计算机】【C语言高频简答题】 ★ 基础

算法和程序的主要区别是什么？

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**完整解答（照着抄一遍，然后合上默写）**：

| 对比项 | 算法 | 程序 |
|--------|------|------|
| 定义 | 解决问题的步骤描述 | 用计算机语言实现算法的代码 |
| 有穷性 | 必须满足有穷性 | 不一定满足有穷性（如操作系统） |
| 表示方式 | 自然语言、流程图、伪代码等 | 特定编程语言 |
| 执行者 | 人/计算机 | 计算机 |

**核心区别**：算法是逻辑步骤，程序是代码实现；算法必须有穷，程序可以无限循环。


</details>

### 10. 【C语言】【自增自减运算符】 ★ 基础

以下程序输出是（ ）

```c
int i = 5;
printf("%d\n", i++);
printf("%d\n", i);
```

A. 5 和 5 B. 6 和 5 C. 5 和 6 D. 6 和 6

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：C

**解析**：

`i++` 先返回 5，再加 1。第一个 printf 输出 5，之后 i=6，第二个输出 6。


</details>

### 11. 【C语言】【选择结构】 ★ 基础

以下程序输出是（ ）

```c
int i;
for (i = 0; i < 5; i++) {
    if (i == 2) continue;
    if (i == 4) break;
    printf("%d ", i);
}
```

A. 0 1 2 3 4 B. 0 1 3 C. 0 1 3 4 D. 0 1 2 3

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：B

**解析**：

i=0 打印，i=1 打印，i=2 continue 跳过，i=3 打印，i=4 break 退出。输出 0 1 3。


</details>

### 12. 【计算机】【2.7 查找】 ★ 基础

通过折半查找对关键字序列 {12, 18, 21, 35, 45, 55, 66}，查找 21 需要查找（ ）次

A. 4　B. 3　C. 2　D. 1

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：B

**解析**：

1. mid = (0+6)/2 = 3 → `a[3]=35`，21 &lt; 35 → 左半  
2. mid = (0+2)/2 = 1 → `a[1]=18`，21 &gt; 18 → 右半  
3. mid = (2+2)/2 = 2 → `a[2]=21` 找到  

共 **3** 次。


</details>

### 13. 【政治】 ★ 基础

中国式现代化与西方现代化的本质区别在于？
A. 发展速度不同
B. 发展规模不同
C. 中国式现代化是社会主义现代化，西方现代化是资本主义现代化
D. 发展方式不同

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：C

**解析**：

中国式现代化是中国共产党领导的社会主义现代化，而西方现代化是资本主义现代化，这是两者本质区别。


</details>

### 14. 【C语言】【改错题】 ★ 基础

求 1 到 n 的和（找出并改正下面代码中的错误）

```c
#include <stdio.h>

int sum(int n)
{
    int i, s = 0;
    for (i = 1; i < n; i++)   /**** 错误 1 ****/
        s = s + i;
    return s;
}

int main()
{
    int n = 10;
    printf("%d\n", sum(n));
    return 0;
}
```

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：`i < n` → `i <= n`

**解析**：

求 1 到 n 的和，循环应包含 n。`i < n` 只加到 n-1，结果为 45 而非 55。


</details>

### 15. 【计算机】【C语言部分】 ★ 基础

以下程序输出是（ ）

```c
int a = 5, b = 10;
int *pa = &a, *pb = &b;
*pa = *pb;
printf("%d %d", a, b);
```

A. 5 10　　B. 10 10　　C. 10 5　　D. 5 5

> 🧭 **先想 30 秒**：这题是基础题，直接照抄解答再默写一遍即可。　👉 想满 30 秒还不动，就展开下面的解答照抄一遍，合上再默写。

<details>
<summary>💡 看完整解答与讲解</summary>

**答案**：B

**解析**：

`*pa = *pb` 表示把 `pb` 指向的值（`b=10`）赋值给 `pa` 指向的变量（`a`），所以 `a` 变成 10，`b` 不变。

`pa` 指向 `a`（就像门牌号写着"a的家"），`pb` 指向 `b`（"b的家"）。`*pa = *pb` 就是"去b的家看看值多少钱，然后去a的家也改成这个数"——a的值变了，但b没变，pa和pb指向的地址也没变。


</details>

> 分布：高等数学 2 · 计算机 7 · C语言 5 · 政治 1

共 15 题。错题登记：docs/checklists/错题本模板.md
重抽一次（换题）：node scripts/daily-drill.mjs --total 15